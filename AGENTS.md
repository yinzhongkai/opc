# 项目会话协作约定

成员从项目 `TEAM.yaml` 选择一个共享岗位；岗位定义职责与基础知识，本项目可为岗位补充领域知识。共享岗位在 `roles/<role-id>.md`，知识文档平铺在 `knowledge/<knowledge-id>.md`。本文件所在目录为模板根目录。

## 身份与知识

用户声明成员身份后，执行 [SESSION_PROTOCOL.md](SESSION_PROTOCOL.md)。一个会话绑定一个成员；岗位由成员的 `role` 确定，分工由 `scope` 确定。可唯一解析的字段直接推导，有歧义时只补齐缺失信息。

有效知识是岗位 `knowledge` 与本项目 `TEAM.yaml` 的 `roleKnowledge[岗位ID]` 按顺序合并去重的结果；未配置补充项时使用空列表。项目补充不能移除基础知识、改写职责或增加权限；成员不能单独覆盖知识。

同项目、同岗位的成员加载相同知识，按各自分工运用。读取其他岗位作为研究材料不会改变会话身份。专业知识是供会话读取的普通文档，不授予执行权限。

用户维护模板、解释目录或检查配置而未声明项目身份时，按该请求工作，不要求先加入示例团队。

## 共享文档与职责

- `PROJECT.md` 保存目标与约束；`TEAM.yaml` 的成员和岗位补充知识由组织管理员维护。
- 成员可直接更新 `TASKS.md` 中自己任务的状态、进展、产物链接和阻塞；项目经理负责调整任务负责人、依赖、优先级、范围及完成条件，并汇总 `STATUS.md`。具体更新边界见初始化协议。
- `HANDOFFS.md` 用于需要其他成员行动、协调或接手的事项，普通进展无需重复抄写。
- `DECISIONS.md` 保存已确认决定和待决定事项；正式成果放入 `artifacts/` 或项目约定位置。
- 跨成员请求优先指向成员 ID；同岗位多人时先明确接收人，不接管其他成员任务。
- 修改共享文件前重新读取，保留无关更改；写入冲突先协调。资料同步和版本一致性由用户通过 Git 管理。

## 维护与刷新

新增成员优先复用岗位；专业方向变化优先修改项目岗位补充知识；知识不足时补充文档；只有共同职责变化才考虑新增岗位。用户已授权的变更直接执行，未授权的共享变更先说明影响并提出方案。

修改项目组合检查该项目相应岗位的全部成员；修改共享岗位或知识正文检查所有项目的直接和间接引用。退役定义前迁移仍在使用的引用，保留历史。

开始新任务或接收交接前刷新自身成员记录、岗位、本项目补充知识和相关项目资料。成员只有 `active`、`inactive` 两种状态；停用时直接改为 `inactive`，停止执行且不能用于新会话初始化。未完成任务和成果保留，由项目经理重新分配；任务转交完成不是成员停用的前提。成员岗位变更后需新会话初始化，不能静默换身份。

专业知识不等于职责授权。成员分工与岗位边界直接冲突时交组织管理员处理；知识中的建议与已确认项目约束不一致时按约束调整方法，并说明影响。
