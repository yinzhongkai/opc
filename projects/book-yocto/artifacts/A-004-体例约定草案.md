```text
项目：book-yocto
成果 ID：A-004
负责人：writer
关联任务：T-005
版本：0.1
更新日期：2026-09-19
状态：draft
适用范围：《Yocto BSP 实战》全书体例约定草案——章节标题层级、节编号、交叉引用、中英文术语书写、代码与输出块、提示框、标点、git tag 标注；确认后作为 P3 逐章修订与统稿依据，本身不改动任何章节正文
来源及输入版本：D-005（2026-09-18 用户确认）；书稿 workspace/yocto/ 当前版本（2026-09-19）；A-001 §3.2、A-003；体例事实经全稿定向统计核实（见各条"现状"）
批准依据：尚无（待用户确认）
版本记录：2026-09-19 v0.1 首次起草
```

# 体例约定（草案）

> 本草案每条给出**现状**（经全稿统计核实的实际情况）与**约定建议**。用户确认后成为统稿依据；标注"【请裁决】"的条目是存在真实分歧、需要你拍板的地方，其余为低争议项。

## 1. 章节标题层级

**现状**：边界章（前言/序章/尾声）章标题用 H1（`# 前言`），节用 H2（`## 1. xxx`）；16 个技术章与 3 个附录用 H2 作章标题（`## 1 把 Yocto 跑起来`），节 H3、小节 H4。

**约定建议【请裁决】**：

- **方案 A（推荐）**：全部章标题统一 H1，节 H2、小节 H3。Markdown 语义上每个文件即一章，应有且仅有一个 H1；GitHub/编辑器预览下章名也最醒目。改动为机械升级（19 章），建议**不单独批量执行**，并入 P3 各章修订时顺带完成，避免产生遮蔽实质修订的大面积 diff。
- 方案 B：边界章降为 H2（与正文一致）。改动最小（约 20 行），但章标题与节标题视觉上只差一级，章节边界不够醒目。
- 推荐 A 的理由：D-002 已定 Markdown 终稿即交付、无排版转换环节，标题层级就是最终呈现层级，值得一次到位。

## 2. 节编号风格

**现状**：技术章节号带章号前缀（`### 9.3`，小节 `#### 9.3.2`）；边界章用无前缀序号且带点（`## 1. 又一年`）；附录用字母前缀（`### A.1`）。技术章的 `N.x.y` 编号被全书大量交叉引用（如 9.3.2、5.6.1），**不可变动**。

**约定建议**：

- 技术章 `N.x(.y)`、附录 `X.y` 保持现状。
- 边界章序号统一为**数字后不加点**（`## 1 又一年`），与技术章章标题"数字后空格无点"的风格对齐。改动 3 章约 18 行，同样建议并入各章修订时顺带执行。

## 3. 交叉引用格式

**现状**：全书以 `chapter N` 为主（统一良好）；节级引用同章内用纯节号（`9.3.2`），跨章用 `chapter 4 的 4.6 节`。

**约定建议**：

- 跨章引用一律 `chapter N`；边界章用名称（序章/尾声/附录 A）。
- 节级引用：同章内纯节号，跨章 `chapter N 的 X.Y 节`——维持现状，写成规则。
- 旧式"第 N 章"清理：经逐处核对，真正的旧式交叉引用只有 **1 处**——task02:417 对话中"那是第三章的事"应改为"那是 chapter 3 的事"（A-001 原报 2 处，另一处 task12:619"排在'做工程'的第一章"是对话修辞、非引用，建议保留）。P3 修订 task02 时处理。

## 4. 中英文术语书写

**现状**（全稿统计）：配方 600 / recipe 388，层 265 / layer 390，poky 443 / Poky 44，bitbake 463 / BitBake 120。实际用法已形成稳定规律，建议把惯例写成明文：

- **首释给对照，后文用中文**：术语首次出现写 **中文（English）**（如 层（Layer）、配方（recipe）），之后行文用中文名。
- **代码、命令、变量、文件路径、程序输出一律英文原样加反引号**，不翻译、不"修正"大小写。程序输出按真实输出逐字引用（例：task02:48 引用的 `Do not use Bitbake as root` 属输出引用，出版前与真实报错核对，不适用体例改写）。
- **poky / Poky 分工**：行文指参考发行版用 Poky；路径、分支、仓库名用 `poky`（小写反引号）。现状已如此，写死。
- **bitbake / BitBake**：命令写 `bitbake`，行文指工具写 BitBake。
- **Yocto**：指项目本体写"Yocto 项目"，泛指生态可单写"Yocto"，英文全称 "Yocto Project" 只用于正式名称场合（前言、延伸阅读）。
- **缩写**：BSP、SDK、eSDK、CI、OTA、TF-A、UBI 等首次出现给全称或中文解释（现状基本如此）。

## 5. 代码块与输出块

**现状**：全书 773 个代码块全部带语言标记，分布：bash 337 / text 290 / bitbake 123 / diff 6 / c 5 / python 4 / ini 4 / yaml 1 / makefile 1 / make 1 / cmake 1。

**约定建议**：

- 语言标记保持"每块必标"；`bitbake` 是自定义标记但全书一致，保留。
- 统一 `makefile`：现有 `make` 1 处（task19）与 `makefile` 1 处（task19），统一为 `makefile`。
- 文件内容块首行注释 `# 文件路径：<绝对路径>` 的现行约定写为明文规则。
- 输出块标题：基线 `输出：`；需限定时用括号补充——`输出（关键行）：`、`输出（示意形态，以实测为准）：`。现状已如此，写为规则；**示意输出的标注必须保留至实测回填完成**（衔接 D-001=A 的回填纪律）。

## 6. 提示框三级

**现状**：💡 提示 55 处、⚠️ 注意 59 处、🔥 重要 12 处，用法一致。

**约定建议**（写为明文）：💡 提示=补充理解与背景；⚠️ 注意=易错点与边界条件；🔥 重要=不可违反的操作纪律（如 root 构建、私钥进仓库、devtool finish -f）。不新增级别。

## 7. 标点与排版

**约定建议**（现状基本已如此，写成规则）：中文行文用全角标点；对话与正文引号用弯引号 ""；破折号用 ——；代码与命令内半角；中英文之间留空格。**文件名映射**：文件名中的引号因文件系统约束用下划线占位（`让芯片_上电_.md`），正文标题用弯引号，index.md 表格按此映射对齐（T-004 已修复）。

## 8. 章末 git tag 标注

**现状**：正文 `chapter1`–`chapter16`，边界章 `prologue`/`epilogue`，附录 `appendix-a/b/c`，均在章末标注；**前言是唯一无 tag 标注的一章**，而前言承诺"每章末尾都有 git tag"。

**约定建议【请裁决】**：前言末尾按同款格式补一行 `> 本章对应 git tag：preface`。归属建议打在 poky（前言时点 meta-tiger 尚不存在，与 prologue、chapter1/2 同库——附录 C 的 tag 归属表需同步补一行）。在前言"怎么读这本书"一节的 tag 说明中补上附录命名（`appendix-x`）。

## 9. 耗时与数字表述

**约定建议**（现状已如此）：耗时写量级并给依据（"约 1-3 小时，视网络与 CPU 而定"）；未经实测的数字必须带可见的"实测回填/以实测为准"标注，直至回填完成。

## 10. 效力与执行方式

- 本约定经你确认后作为全书统稿依据（成果状态随之更新）；P3 各章修订时逐章对照执行，T-006（chapter 1 试点）首次试用并回头校准本约定。
- 标题层级与节编号类机械改动并入各章修订顺带执行，不做独立批量改写（避免 diff 遮蔽实质修订）。
- 术语表（glossary）不在本仓库（A-003 X-2）；建议 P1 重建术语表载体后，本约定第 4 节作为其书写规范。
