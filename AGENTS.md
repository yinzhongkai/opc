# 项目会话协作约定

本文件所在目录是框架根目录，管理边界是当前克隆的仓库。一般一个业务项目使用一份独立克隆；公共修改不会自动同步到其他克隆。按用户请求区分超级管理员入口和项目成员工作，项目及团队都可以为空。

## 操作入口

- 超级管理员：执行根 [SUPER_ADMIN.md](SUPER_ADMIN.md)，负责框架维护、建项、成员文档及知识配置。它不是项目岗位，不登记在团队中，也不绑定项目成员。用户声明“你是超级管理员”，或直接要求建项、创建成员时，使用此入口；实际写入仍以本次授权范围为限。
- 项目创建：超级管理员按 [PROJECT_PROTOCOL.md](PROJECT_PROTOCOL.md) 从 `templates/project/` 建立项目；只创建用户要求的成员，不默认配齐岗位。
- 成员工作：执行 [SESSION_PROTOCOL.md](SESSION_PROTOCOL.md)，从实际项目 `TEAM.yaml` 的成员 ID 定位 `members/<member-id>.yaml`。用户说“你是项目经理”等自然语言时，唯一匹配即绑定，歧义时只补问必要信息；会话标题不能代替身份声明。
- 解释目录、检查配置等只读请求直接按请求处理，无需加入团队。配置结构以 [CONFIG_SCHEMA.md](CONFIG_SCHEMA.md) 为准。

## 执行约束

- 用户保证当前仓库每次只有一个会话执行，包括超级管理员和所有项目成员；前一会话完成或停止后再启动下一会话。框架不实现文件锁或自动调度。
- 成员只能使用已创建并登记的身份，按岗位与 scope 执行获授权工作；知识提供方法，不增加职责或权限。配置及共享定义变更由超级管理员按授权处理。
- 开始新任务、评审或接收交接前按会话协议刷新；身份与岗位知识必读，当前工作按关联条目读取，无关历史按需追溯。修改文件前重读目标，局部修改并保留无关内容，发现冲突先协调。
- 评审由现有成员在原会话执行，默认在原任务中记录。用户保证受评版本可读取且不变，本轮全部评审者提交意见后作者再统一修订；评审完成不等于全部通过，更不等于成果已批准。

## 规则归属

身份解析、知识加载与刷新以 [SESSION_PROTOCOL.md](SESSION_PROTOCOL.md) 为准；字段和引用以 [CONFIG_SCHEMA.md](CONFIG_SCHEMA.md) 为准；共享文件责任、任务、评审、决定和交接以 [PROJECT_PROTOCOL.md](PROJECT_PROTOCOL.md) 为准。岗位保留专业职责，知识保留工作方法，平台适配说明只处理接入与读写条件，不另行复制或改写公共流程。
