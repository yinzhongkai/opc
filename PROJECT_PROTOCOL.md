# 项目创建与运行协议

本协议定义空框架建项、创建成员、共享文件维护和工作流。框架级管理见 [SUPER_ADMIN.md](SUPER_ADMIN.md)，项目成员身份加载见 [SESSION_PROTOCOL.md](SESSION_PROTOCOL.md)，结构字段见 [CONFIG_SCHEMA.md](CONFIG_SCHEMA.md)。根 [AGENTS.md](AGENTS.md) 是统一入口。

## 使用前提

当前仓库采用用户控制的会话串行执行：包括超级管理员和项目成员在内，每次只有一个会话执行，前一会话完成或停止后再启动下一会话。用户负责执行顺序和资料同步，框架不实现文件锁、自动调度或并发写入协调；各会话仍须刷新资料、写前重读并局部修改。

## 1. 从空框架创建项目

用户明确要求创建项目后，超级管理员以当前仓库的框架管理入口执行以下步骤，不需要先有项目成员：

1. 从请求确定项目名称、唯一 ID、目标和首批成员需求。能合理推导的目录 ID 直接采用并在结果中说明；名称或目标尚有未知项可标为待确认，不虚构需求、期限和预算。
2. 检查 `projects/<project-id>/` 是否已存在。存在时读取并确认是继续维护还是新建，不覆盖已有项目。不得恢复用户删除的项目来充当新模板。
3. 将 [空白项目模板](templates/README.md) 的全部文件复制到新项目目录，替换 `{{project_id}}` 和 `{{project_name}}`，填写实际建项日期和用户请求来源。模板与项目均位于根目录下两级，相对根链接可直接复用。
4. 首批成员未指定时使用版本 3 空团队 `members: []`，不预置超级管理员或项目经理。用户指定成员需求时按第 1.1 节建立成员文件、登记索引、填写必要的 roleKnowledge。新增配置不等于已创建会话。
5. 在 PROJECT 中记录最终确认人和协调记录维护人。默认最终确认人为建项用户，记录其可辨认身份或本次请求来源；无成员时协调记录维护人待指定，不把超级管理员写成业务成员。配置项目经理时按第 1.1 节明确记录责任；用户另有指定时遵从其指定。
6. 只记录实际授权的任务和决定。空任务表、空交接表是有效起点；模板中的格式样例不转成真实待办。建项事实记入 PROJECT，不必人为增加一个已完成的“建项任务”。
7. 检查字段、引用和所有占位符均已处理，运行结构校验（工具不可用时手工逐项核对并说明范围）。返回实际目录、配置成员、未知项、检查结果和成员启动消息。

建项完成只代表文件和配置可用。超级管理员会话继续作为框架入口；用户另开成员会话后，再按初始化协议绑定身份。创建框架模板本身不应在 `projects/` 中留下示例业务项目。

### 1.1 先创建成员，再启动会话

1. 用户先向超级管理员提出“为这个项目创建项目经理、产品经理和系统架构师”等请求。只有一个实际项目时可以推导；多项目未明确目标时先询问，不跨项目猜测成员归属。
2. 读取 PROJECT、TEAM、全部已登记成员文件、相关任务与交接以及候选岗位、知识，核对当前人员和职责。已有可用成员时明确告知，不因用户准备打开新会话就重复创建；是否新增同岗位成员按用户实际需求决定。
3. 复用 active 岗位，生成项目内唯一且不复用历史记录的成员 ID。岗位需求明确而分工未细化时，可以用该岗位共同职责作为初始 scope 并在结果中说明；多人同岗的职责重叠、专业方向或授权范围有实质歧义时询问，不虚构任务和项目约束。
4. 按配置规范创建 `members/<id>.yaml`，只写 id、role、scope；将 ID 加入 TEAM.members，必要时调整项目岗位补充知识。TEAM 不重复成员字段，成员文件不复制岗位或知识正文。文件和索引属于同一次配置变更，写前重读、串行完成；中断或校验未通过时报告未完成，不能将半配置状态交付为可绑定成员。
5. 若新建的是首名项目经理，且 PROJECT 的协调记录维护人仍为默认待指定、用户没有其他安排，则将其明确指定为维护人并说明此默认职责；已有用户指定时不覆盖，多名候选有歧义时询问。超级管理员只记录本次建员和责任配置事实，不接任长期项目协调。
6. 完成字段、索引、引用和本地链接检查。返回创建或复用的成员 ID、岗位、scope、配置路径、知识组合，以及简短启动消息。单项目单岗位成员可使用“你是项目经理，请初始化”；存在歧义时消息包含实际项目名称或 ID、成员 ID。
7. 用户打开新会话发送该声明，由新会话实际读取配置、岗位和知识并返回初始化回执。成员文档已保存不等于会话已创建、绑定或开始工作。

配置变更在 PROJECT 的“成员配置记录”保留日期、用户请求来源、涉及成员 ID、创建/复用/岗位或分工变化和配置路径；正文以当前成员文件为准，记录不另建一份动态成员表。框架管理员署名“框架超级管理员”，不伪造项目成员 ID。

## 2. 共享文件的依据与维护责任

| 文件 | 保存的信息 | 维护责任 |
|---|---|---|
| `AGENTS.md` | 本项目身份、公共规则入口和资料位置 | 超级管理员维护入口，避免复制动态成员表或进展 |
| `TEAM.yaml` | 成员 ID 索引、项目岗位知识组合 | 框架超级管理员 |
| `members/<id>.yaml` | 唯一成员身份配置：id、role、scope | 框架超级管理员 |
| `PROJECT.md` | 当前目标、约束、确认人、记录维护人、交付约定与配置记录 | 协调记录维护人依据确认内容整理；框架管理员按授权登记建项及配置事实 |
| `TASKS.md` | 已登记任务、负责人、计划字段、实际进展及轻量评审 | 登记者按第 3 节受理；负责人更新任务事实；评审者按第 4.1 节更新自己的意见与复核；项目经理维护团队计划 |
| `DECISIONS.md` | 待决定问题、确认结果、确认来源及替代关系 | 提出者登记问题；实际确认人决定；记录人据证据写回，协调记录维护人整理索引 |
| `HANDOFFS.md` | 需要其他成员或用户行动的请求和接收结果 | 发起人登记，目标成员接收和反馈，发起人核对后关闭 |
| `STATUS.md` | 截至某一时间的总体进展和下一步 | 协调记录维护人依据任务、决定及交接汇总 |
| `artifacts/README.md` | 成果索引、版本、状态、负责人和任务链接 | 成果负责人更新本人条目，协调记录维护人检查索引一致性 |
| 成果正文 | 具体内容、适用范围、版本和来源 | 对应成果负责人 |

协调记录维护人可为 PROJECT 中明确指定的一名在册成员，或在尚无安排时写待指定；有多名项目经理时也只指定一个人维护摘要，用户可以明确指定其他在册成员。该成员承担记录整理，不因此获得其他岗位的专业职责或自行批准范围的权力。该职责暂缺时摘要标待维护，不阻止建项、创建成员、其他有效成员初始化或执行已授权任务。超级管理员只记录实际建项和配置事实，不作为长期维护人填补空缺。

当项目经理未配置或其会话未运行时，用户确认跨成员计划变化，由用户指定的在册成员记录该次已确认变化；如需持续代为整理，更新 PROJECT 的记录维护人。已授权任务仍可由负责人继续处理。

### 冲突与更新

- 按信息种类查原始记录：成员名单与项目知识看 TEAM，成员身份和分工看对应成员文件，任务看 TASKS，决定看 DECISIONS，内容看对应版本的成果。STATUS 是摘要，不覆盖这些记录。
- 最新用户明确指令若尚未写回，记录其来源并更新对应文件；不得仅因某个文件更新时间较晚就认定其推翻了已确认决定。
- PROJECT 的当前目标或约束改变时，链接相关决定；批准、替代成果时更新其正文和索引，涉及其他维护人的文件则登记具体交接。
- 普通进展只写任务记录；摘要注明汇总时间和信息截至点。尚未同步的摘要标明待更新，不让旧摘要成为业务阻塞。
- 写前重读，局部修改，保留历史和无关内容。发现未预期的文件变化时重新核对；冲突未解决前停止该处写入并告知用户。

## 3. 任务受理、登记与完成

任务进入项目有两种方式：项目经理在已授权范围内安排团队计划；用户直接指派给当前成员、且符合岗位和 scope 的工作由该成员登记。后者可立即登记授权来源和本人任务，不必等项目经理转录。

成员可自行分解已授权任务中的执行步骤。涉及他人负责人、全局优先级、跨任务依赖、范围或完成条件的变化，由项目经理在权限内安排，超出已确认范围的由用户确认；无项目经理时由用户确认。接收交接本身不等于已获任务指派权。

任务使用项目内唯一的 `T-001` 等编号，登记者写前读取现有编号并取下一号；发现重复编号时先核对记录及引用，再修正。不得复用被取消任务的编号。

每条任务包含：标题、负责人、状态、授权来源、目标与范围、输入/依赖、优先级（未明确时写未设定）、完成条件与确认方式、进展、成果/证据、阻塞与下一位行动人。日期和来源使用实际信息。格式见 [TASKS 模板](templates/project/TASKS.md)。

| 状态 | 含义及转换条件 |
|---|---|
| `todo` | 已受理且负责人明确，尚未开始 |
| `in_progress` | 负责人正在执行 |
| `blocked` | 明确条件使任务无法推进，记录原因和解除条件；解除后回到 todo 或 in_progress |
| `in_review` | 完成条件要求的评审或验收尚待完成；开始修订时回到 in_progress，交叉评审须先完成本轮全部评审 |
| `completed` | 约定完成条件已满足并有证据；要求外部确认的任务须有实际确认 |
| `cancelled` | 用户或有相应权限的项目经理取消，记录原因与已有成果，保留整条历史 |

负责人可更新本人任务的状态、进展、证据和阻塞，不能靠降低完成条件将任务标为完成。任务由新负责人接手后，由新负责人维护，保留原负责人和转交记录。重新打开 completed/cancelled 任务需项目经理或用户确认并记录原因。

轻量评审是任务内的有界协作：获安排的评审者可填写自己的意见和复核条目，原作者可填写问题处理回复；这不转移任务负责人，也不授予修改他人意见、整体状态、范围或完成条件的权力。具体安排与记录见第 4.1 节。

“形成候选稿”的任务可在草拟完成后 completed，而成果仍为 draft；以“获得批准”为完成条件的任务则须等待确认。并非每个客观检查或草拟任务都必须独立评审，评审要求应在任务完成条件中明确。

尚未明确验收要求但影响结果时应尽早澄清；不因未配置评审岗位而把作者自查写成独立评审。确认人默认取 PROJECT 中当前明确的默认最终确认人，获明确授权的其他确认人可在任务中指定。

## 4. 决定与成果批准

决定使用 `D-001` 等唯一编号，记录问题、状态、提出者、确认人、来源与日期、适用范围、影响和关联任务/成果。待决定项的确认人可写待指定，但必须在最终确认前明确；未指定时按 PROJECT 的默认确认人处理。

决定状态为 `proposed`、`confirmed`、`superseded`、`rejected`。只有实际确认后才转为 confirmed；替代时新决定引用旧编号，旧决定标 superseded，保留原文和确认来源。被否决建议保留为 rejected，不静默删除。

来源应包含可辨认的用户/确认人、日期以及足够复核的请求摘要或消息链接。记录人可以依据已有明确授权写回决定，不必重复请求相同确认；记录操作不构成替用户作决定。

成果负责人标明项目、`A-001` 等成果 ID、关联任务、负责人 ID、版本、更新时间、适用范围和来源；依赖其他成果时注明输入版本。缺少已确认任务时，先按受理规则登记本人任务。

成果状态为 `draft`、`in_review`、`approved`、`superseded`。提交后进入 in_review，开始修订时回到 draft；交叉评审按第 4.1 节先完成本轮全部评审再修订。获确认后进入 approved，记录决定编号与被批准的版本。新版本替代后保留版本记录和替代决定，不把旧批准状态自动复制给新内容。

评审的 `pass / revise / blocked` 是针对特定版本及声明范围的结论，是否足以批准由任务和 PROJECT 的确认约定决定。作者自查、专业交叉评审、独立评审者审查和用户批准应如实区分。

每份成果默认维护一份当前正文。修订时更新版本记录；重要批准/交接记录引用版本及可获得的 Git 提交或内容摘要。历史内容通过用户维护的 Git 追溯；未入 Git 的内容不宣称可以恢复。项目可另行约定必要的发布物和外部证据存放方式。

### 4.1 原成员会话中的专业交叉评审

交叉评审是现有成员的共同协作方式，不默认新增岗位或会话。成员只在自身岗位及 scope 的专业范围内评审他人成果；阅读产品、架构等其他成员文档不改变身份，也不授予改写其原稿或替其确认的权力。作者自查不能冒充他人评审，单会话模拟多个角色也不表示这些成员实际参加。

本轮受评版本的可读取性和稳定性由用户保证：本轮选定的全部评审者提交意见前，作者不修改受评正文；全部意见提交后再统一修订。“评审完成”不要求结论全部为 pass，revise 或 blocked 也应如实记录。框架不强制为每轮评审保存快照或提交 Git；修订后的复核同样先完成本轮复核，再进行下一次修订。

1. 作者完成自查并提交明确版本，注明希望评审的范围和相关上下游资料。需求定稿、架构确定、重大变更或阶段交付可安排交叉评审；小改动是否需要评审按任务完成条件判断，不要求全员评审每份文档。
2. 项目经理在授权范围内，或用户，在原任务中记录本轮评审安排：授权来源与日期、受评成果路径与版本、轮次、选定的在册成员、各自范围和完成条件。这构成相应评审工作的明确安排，无需为每位评审者另建任务；作者提交稿件本身不授予指派他人的权力。没有相关任务时先按第 3 节新建一条承载本轮的评审任务；原任务已 completed/cancelled 时须新建任务或经有权者明确重开，不能静默追加工作。
3. 用户依次进入各评审成员原会话触发处理。评审者按刷新协议读取安排、受评文件及依赖版本，缺少证据则明确 blocked 或未覆盖范围；不能声称看到别的工作副本尚未同步的文档。默认直接在原任务的本轮评审小节署名记录意见，不要求每人建立独立报告、成果索引条目或重复交接单。
4. 每位评审者记录实际成员 ID、日期、评审范围与依据、未覆盖项和结论；没有问题时也须有明确结论。问题记录本轮内唯一编号、具体位置、依据与影响、修改建议、原稿责任人；作者填写处理回复，实际复核者补充复核版本、结果和证据。每人只写自己的意见、回复或复核，不代写他人的接收与结论。格式见 [任务模板](templates/project/TASKS.md)。
5. 本轮全部评审者提交意见后，原作者汇总处理问题并统一更新原稿版本，说明已修订、存在分歧或建议不采纳的理由。相关评审者复核后记录解决结果；项目经理协调未解决分歧，涉及已确认范围或最终批准的事项交有权确认人决定，不以简单多数票或进度压力覆盖专业证据。
6. 评审结论限定受评版本与范围。修订后旧 pass 不自动覆盖新版本；按变更影响复核，保留前轮意见及对应版本，不覆盖旧结论。轻量评审记录不是单独成果，不额外走批准流程；原任务是否完成、交接是否关闭、原成果是否批准仍按各自条件与实际证据判断。

范围复杂、需要单独排期或交接、要求独立交付报告，或用户明确要求时，可将评审拆成独立任务并将报告放入成果目录；相应交接与成果按普通流程管理，不默认要求报告再接受一轮递归评审。已明确的报告、独立审查或验收要求不能擅自降级为轻量记录。轻量与正式记录都遵守本节的身份、版本和修订顺序。

例如：架构师评需求的可行性，测试成员评验收条件是否可验证，产品经理评架构是否遗漏业务目标。可选 reviewer 岗位仍可用于用户另行要求的独立审查，但本流程不依赖它。

## 5. 交接与接收

只有需要其他成员行动、协调或接手时才登记交接；第 4.1 节已在任务中记录清楚的评审/复核安排无需重复登记，另有补充输入、协调或责任转交请求时再建交接。交接使用 `H-001` 等唯一编号，记录发起人、一个明确的目标成员 ID（或用户）、关联任务、期望结果、输入成果及版本、未完成事项、状态和反馈证据；多个接收人分别登记。管理员在已授权配置工作中向成员发请求时，发起人注明“框架超级管理员”和用户授权来源，关联任务可写无及原因；这不赋予业务任务指派权。成员需要管理员处理时目标写用户、期望结果注明转交超级管理员，不构造管理员成员 ID。

| 状态 | 更新责任与条件 |
|---|---|
| `open` | 发起人登记，尚未收到接收确认 |
| `accepted` | 目标成员实际读取后确认接收，记录日期与处理范围 |
| `closed` | 目标成员提供结果，发起人核对期望结果后关闭并记录证据 |
| `cancelled` | 发起人或用户撤销请求并记录原因；已接收时同步说明对相关任务的影响 |

目标为用户时，由发起人依据用户的实际答复记录接收与结果，不代写未经发生的确认。closed/cancelled 后新增工作建立新交接并链接旧记录，不改写原处理结果。

接收交接前按初始化协议刷新资料；超出 scope、输入缺失或未获任务授权时，保留 open 并记录需要谁决定或补齐。接收不自动转移任务负责人，任务转交须由项目经理或用户安排。

交接不会自动启动另一个会话。发起人在回复用户时指出下一位行动人和交接位置，用户可进入对应会话继续；无需为了每条普通进展新增交接。

## 6. 团队变更与项目结束

成员与知识配置由超级管理员维护，检查范围以配置规范为准。岗位变化需要新会话初始化，scope 或知识变化在原身份下刷新；配置变更按第 1.1 节留下授权与变更记录。

项目经理或用户可以明确安排在册成员之间的任务、交接或成果维护责任转交，记录范围、授权来源和日期。新的交接发起人可核对并关闭交接，新的接收人需实际读取并确认接收；新的成果负责人维护后续版本，保留原作者和批准记录。转交不自动完成任务、关闭交接或批准成果，也不代写原成员确认。

项目结束时按实际结果整理任务和交接，不将未完成或取消的工作标成 completed。协调记录维护人依据用户确认更新项目阶段和最终摘要。若项目始终为空团队或记录维护人暂缺，超级管理员可仅按用户明确结束/取消授权登记生命周期事实及已有记录摘要，不伪造业务验收，也不因此接任长期项目经理。项目归档、删除和资料发布分别按用户授权处理，结束项目不自动删除文件或更改团队名单。
