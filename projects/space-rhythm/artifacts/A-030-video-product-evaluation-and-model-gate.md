# 视频产品效果、性能与可选模型门禁评估

- 项目：space-rhythm
- 成果 ID：A-030
- 负责人：video-algorithm-engineer-cv-01
- 关联任务：T-029
- 版本：0.1
- 更新日期：2026-09-14
- 状态：draft
- 适用范围：代表产品视频、人工标注、经典视频算法效果/性能和“卡点自然”评估，以及可选模型门禁；不批准产品口径，不把合成样本替代产品数据，不直接引入 ONNX 或修改核心时间线。
- 来源及输入版本：用户于 2026-09-14 明确授权启动 T-029，并要求纳入代表产品视频与人工标注、“卡点自然”评估口径、基准硬件和效果/性能阈值；D-001～D-008 confirmed；A-002 0.2、A-016 0.1、A-028 0.2、A-029 0.1；T-028 提交 `0513aa3`。
- 批准依据：尚无。
- 版本记录：2026-09-14，0.1，登记 T-029 启动、输入就绪审计、评估协议草案和模型门禁边界；没有产品效果或性能通过结论。

## 1. 输入就绪结论

用户本次消息已解除“未授权启动”条件，但没有附带第 2～4 项的具体值。仓库检查只发现
T-028 的 10 项项目自制合成 golden 和媒体契约样本，没有发现可识别为代表产品集的视频或
对应人工标注；项目决定和成果中也没有已确认的自然度 rubric、基准硬件元组或效果/性能
阈值。机器可读审计见
[`input-readiness-v1.json`](../evidence/T-029/input-readiness-v1.json)。

| 输入 | 当前事实 | T-029 判定 |
|---|---|---|
| 启动授权 | 用户于 2026-09-14 明确授权 | `confirmed` |
| 代表产品视频 | 0 条；合成 golden 不属于产品集 | `missing_confirmed_input` |
| 人工标注 | 0 份产品标注 | `missing_confirmed_input` |
| “卡点自然”口径 | 未给出 rubric 版本、评分尺度、播放条件或评审规模 | `missing_confirmed_input` |
| 基准硬件 | 未指定；当前主机探测受限且 Wine 不是获批 Windows 基准环境 | `missing_confirmed_input` |
| 效果/性能阈值 | 未给出任何按指标的比较符和值 | `missing_confirmed_input` |

因此 T-028 的合成结果仍只能作为契约基线 `measured/not-evaluated`。T-029 的产品效果门禁、
性能门禁和模型门禁均为 `not-evaluated`，不能从合成集的零 FP/FN 推导产品通过。

## 2. 代表产品集和人工标注输入契约

产品侧提供的每个 clip 至少登记：稳定匿名 `clipId`、只读位置、内容 SHA-256、来源/使用权限、
时长、分辨率、真实帧率模式、视频流键、内容类别、质量属性和数据分区。覆盖类别至少应从已
确认主流程中选择快切、慢切、全局运镜、局部动作、慢镜头、VFR、近静止、低质压缩及混合
事件；具体 clip 数、时长和各类配额必须由用户/产品侧确认，算法侧不自行补数。

事件标注继续使用 A-028 0.2 的真实 `timeNs` 语义。每项包含 `annotationId`、`clipId`、
`kind=shot|motion_peak|action_peak`、`timeNs`、`durationNs`、前后匹配窗口、标注版本、匿名标注人
和复核人；明确的无事件区间登记 negative span。还需逐次保存新增、删除、移动、改类和锁定
操作，才能计算每分钟人工修正量。媒体、标注和分区 hash 必须在分析前冻结，调参集与最终
评估集不得混用。

产品视频可以保持在用户控制的外部只读目录，证据只登记脱敏位置标识和 hash；不得把私人
素材、`package/` 内容或未知许可作品复制进可分发 golden。

## 3. “卡点自然”rubric 待确认稿

建议采用随机化盲评并保留逐评审者原始记录，不只保留平均分。评价单元是固定 clip、冻结的
候选时间线和固定的试听映射；评审界面、音量、设备、是否显示时间线、播放次数及 classic/
人工参考的呈现顺序都要版本化。建议 rubric 维度为：

1. 事件与可见镜头/动作落点的时间贴合；
2. 被选视觉事件的显著性与节奏重音是否相称；
3. 密度是否造成漏拍或多余干扰拍；
4. 连续片段中的节奏是否稳定且不过度跳变；
5. 达到可用结果所需的人工修正负担。

上述仅为待确认维度，不构成已批准产品口径。用户/产品侧仍须明确评分尺度、是否采用单样本
评分或成对偏好、评审者数量与构成、片段随机化/重复规则、汇总统计、缺失/弃权处理和通过
阈值。确认前只保存原始评分，禁止计算“通过率”。

## 4. 效果和性能门槛待确认项

效果门槛应逐项给出指标、比较符、数值、单位、适用 slice 和聚合方式，至少覆盖：各 kind 的
precision/recall/F1、FP/min、匹配绝对时间误差 P95、渐变区间 IoU、每分钟人工新增/删除/移动/
改类数，以及自然度原始评分或成对偏好。不得只给一个综合平均值掩盖慢镜头、VFR、运镜或
低质量 slice。

性能基准元组至少包含 Windows edition/build、CPU、物理/逻辑核数、内存、GPU/驱动、电源
方案、存储、构建 preset、线程上限、代理尺寸/采样策略、缓存冷热和典型/最大素材。阈值至少
覆盖 analyzed fps 或 real-time factor、wall time P95、峰值 private/working bytes、峰值线程数
和取消延迟 P95。

当前只探测到主机电源方案为“平衡”；CPU、内存、GPU 和 OS 的 CIM 探测返回 access denied。
T-028 的 Wine 8.0 容器数值可复核但不属于已确认 Windows 基准矩阵，不能用作产品性能 pass。

## 5. 可选模型门禁

当前 `modelEvaluationStarted=false`。只有在以下条件同时满足后才允许提出模型方案：

1. 产品数据、标注、rubric、基准机和阈值均有确认来源与 hash；
2. classic 1.0.0 在冻结最终评估集上实际违反至少一项已确认门槛；
3. 失败不是输入质量、标注分歧、媒体映射或运行环境造成；
4. 候选模型能单独报告相对 classic 的效果收益、时间误差、人工修正量、性能、内存、CPU/GPU、
   包体、确定性、模型来源与许可证影响。

满足上述条件也只形成模型引入建议，实际增加 ONNX Runtime、模型文件或发布依赖仍须新决定。
目前没有 classic “未达门槛”的合法结论，故未启动模型评估。

## 6. 解除阻塞所需输入

用户/产品侧需提供一份明确回复或填写等价清单，包含：产品视频只读目录及逐项使用权限、人工
标注文件和标注/复核协议、自然度 rubric 完整版本、基准硬件元组，以及每个效果/性能指标的
阈值。输入齐备后，负责人将冻结 hash、运行 classic、报告 slice/总体分布和人工修正/盲评，
再按第 5 节判断是否仅需调参、确认失败，或提出模型方案。
