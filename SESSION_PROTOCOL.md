# 成员会话初始化与刷新协议

本协议负责项目成员身份与资料加载。框架级超级管理员使用 [SUPER_ADMIN.md](SUPER_ADMIN.md)，不进行成员绑定。项目创建及工作流见 [PROJECT_PROTOCOL.md](PROJECT_PROTOCOL.md)，字段校验见 [CONFIG_SCHEMA.md](CONFIG_SCHEMA.md)。

## 1. 确定场景与身份

先读取根目录 [AGENTS.md](AGENTS.md)。用户说“你是项目经理”“你是太空律动的系统架构师”等，即构成成员身份声明；会话标题只是用户界面的标签，不能单独作为绑定依据。用户说“你是超级管理员”或要求创建成员时使用框架入口；模板中的示例和占位符不能用于成员身份绑定。

1. 从用户声明、已明确的项目上下文和实际目录确定唯一项目。未指定且当前仓库只有一个实际项目时直接推导；存在多个项目时先确定目标，不能因为某岗位只在其中一个项目出现就代替用户选择项目。项目名称可由 PROJECT 中显示名称解析；没有实际项目时提示先由超级管理员建项，不使用模板冒充。
2. 读取目标项目 `AGENTS.md`、`PROJECT.md`、`TEAM.yaml` 和成员目录说明，按配置规范检查版本与索引。用户给出成员 ID 时直接读取已登记的 `members/<id>.yaml`，无需为绑定遍读其他成员；给出岗位 ID 或其显示名称时，读取名单中成员文件以解析候选，只在该岗位恰有一名在册成员时推导。多人同岗时列出 ID 与 scope，只询问所需成员，不按编号最小、最新文件或分工猜测。
3. 未创建对应成员、成员未登记或配置文件缺失时，报告无法绑定及具体缺口，请用户让超级管理员配置；本成员会话不自动新增成员。用户岗位声明与精确指定成员的实际 role 冲突时，不静默改配置或身份。
4. 岗位和完整分工只取自实际成员文件的 `role`、`scope`，文件 id 与索引、文件名必须一致。确认岗位文件 active、知识配置有效。框架入口不是可用的项目岗位，不能绑定 super-admin。
5. 结构检查可借助校验器，但不能代替实际读取成员、岗位和知识正文。按后续分层要求完成必读资料及相关条目加载并返回回执后，才可报告本会话绑定完成。

一个成员会话绑定一个项目中的一个成员。切换成员、项目或岗位需新会话重新初始化；同一身份可以在原会话重新加载。读取或评审其他岗位成果不构成切换身份。

这里的绑定是会话读取配置后采用工作身份，不是平台账号绑定、自动查找旧会话或文件锁。按项目运行协议，用户保证当前仓库每次只有一个会话执行；同一成员换用新会话时，也由用户保证旧会话停止该成员工作。正常初始化不额外建立会话锁或要求重复确认；若已知另一会话仍在执行，告知用户先停止该会话，再继续工作。

## 2. 加载岗位与知识

完整读取 `roles/<role-id>.md`，核对 ID、状态、职责、边界和基础知识列表。读取当前项目 `roleKnowledge[岗位ID]`，缺省为空。

有效知识等于基础列表加补充列表，按首次出现顺序去重。完整读取每份 `knowledge/<id>.md` 的正文，并核对 ID 与 active 状态。索引不能代替正文。

同项目同岗位成员使用相同知识集合，以 scope 区分分工。若 scope 与岗位边界冲突，报告给超级管理员；知识不能用于扩大职责或工具权限。

## 3. 分层加载项目上下文

首次初始化按以下层次读取，不要求遍读所有台账的历史正文：

1. **必读规则与身份**：完整读取根入口、本协议、项目运行协议、配置规范、目标项目入口、`PROJECT.md`、`TEAM.yaml`、`members/README.md`、自身成员文件，以及第 2 节要求的岗位和全部有效知识。前面步骤已完整读取的内容无需重复读取。
2. **当前工作定位**：读取 `STATUS.md` 当前摘要和 `artifacts/README.md` 索引；扫描 `TASKS.md`、`HANDOFFS.md`、`DECISIONS.md` 的条目标题、状态、责任成员及适用范围。不能只查任务负责人：任务内指向本人的评审或复核安排也属于本人工作。
3. **相关条目与输入**：完整读取本人未完成任务、尚待处理的评审/复核、未结交接，以及当前有效的全项目决定、影响本职工作的待决定事项和相关约束。按这些条目的任务、成果、决定和交接引用补齐依赖正文；开始执行具体任务或评审前，读取其所需成果版本和上下游资料。
4. **按需历史**：已完成或取消的任务、已结交接、已替代决定和无关成果，只有在依赖追溯、冲突核对或用户要求时才读正文。保留原记录，不因本次未加载就删除或当作不存在。

可用目录、搜索或分段读取定位条目，但摘要和命中行不能代替相关记录正文。不能遗漏未带本人 ID 的全项目约束；资料缺少可扫描结构、结果被截断或无法确定相关范围时，扩大读取到能够核实为止，不能仅凭未命中声称没有任务。项目经理等协调成员按职责读取团队当前计划，不局限于本人负责的执行任务。

区分已确认决定、任务事实和状态摘要，按项目运行协议处理不一致。旧摘要不覆盖任务原始记录，Draft 不自动成为批准范围，已取消任务不自动恢复。项目运行中协调记录维护人暂缺属于待协调事项，本身不导致其他有效成员初始化失败。

## 4. 返回初始化回执

向用户简洁汇报：

- 项目 ID、显示名称；成员 ID 与实际配置文件路径；岗位 ID、名称及主要边界。明确是否从唯一项目、唯一岗位成员推导。
- 成员实际分工；基础知识、项目补充知识和合并后的有效集合。
- 实际读取的文件路径与范围，区分完整正文、台账中的相关条目和按需未读历史，不把扫描称为全文已读。
- 本人任务与状态、待评审/复核安排、待接收交接、影响工作的决定或阻塞；确无待办时明确说明。
- 初始化结论：完成，或未完成及具体原因。

身份唯一、成员已登记、岗位 active、必需配置有效，且必读资料及已定位的相关条目均已实际完整读取后，才可报告完成；无关历史未读不影响初始化。具体任务输入未齐时注明该任务尚不能执行，不把初始化完成当作已读全部成果。成员在册或启动消息已生成不等同于初始化完成，不能代写其他会话的绑定回执。

## 5. 缺失、失效和冲突

身份不明、字段错误、定义缺失或必需正文不可访问时，报告初始化未完成，列出已确认信息与具体缺口。不要伪造资料或用历史记忆冒充当前文件。

能在用户授权范围内修复的框架或配置问题，可以修复后重新校验。需要用户选择时询问最少信息；需要其他成员行动时按项目协议登记请求。初始化失败不阻止用户明确授权的框架修复，但不能开始依赖缺失身份、职责或知识的业务工作。

若项目目录被移除或不可访问，停止以该项目成员身份执行工作；报告当前事实，不推断用户意图或恢复项目。用户要求维护框架时，按框架维护场景处理。

## 6. 新任务与交接刷新

开始新任务、处理评审/复核、接收交接或恢复中断工作前：

1. 重新读取 TEAM 中自身登记、实际成员文件、岗位、scope 和项目补充知识配置；登记或文件消失则停止依赖该身份的业务工作，交超级管理员核查，不自行恢复旧配置。
2. 核对根规则、岗位和有效知识是否变化；新增或变更正文完整重读。只有已完整读取且通过内容比较或摘要核对确认未变的正文可以复用；无法确认或上下文丢失时重新读取。文件时间不作为唯一判断依据。
3. 按第 3 节分层刷新项目当前事实，重读相关任务内的评审安排、决定、交接和所需成果版本；已经关闭或替代的记录只按依赖追溯，不重新遍读无关历史。
4. 岗位变化时使用新会话初始化；scope 或知识变化则在原身份下刷新并说明影响。

刷新后的方法须符合已确认约束。知识建议与项目约束不一致时说明调整；跨成员职责冲突交超级管理员，范围或计划争议交项目经理或用户。

## 7. 访问与写回限制

本协议是文档协作规则，不授予工具权限。文件由目录、附件或其他方式提供时，应确认它们属于同一项目和可核对版本。

无法写入时输出目标路径、建议修改和依据，明确尚未保存；无法读取必需资料时不能宣称标准初始化完成。具体平台操作见 `adapters/`，公共身份和流程规则仍以根协议为准。
