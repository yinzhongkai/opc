# 项目任务

状态与登记权限见根 [项目运行协议](../../PROJECT_PROTOCOL.md)。

## T-001：书稿现状评估（结构、术语与技术准确性风险清单）
- 负责人：writer
- 状态：completed
- 授权来源与日期：2026-09-12 用户在本会话（writer 初始化后）直接指派："通读 yocto/ 全部 22 章草稿并产出书稿现状评估（结构、术语、技术准确性风险清单）"，用户回复"开始吧"确认。
- 目标与范围：通读 `yocto/` 目录前言、task01–task21 共 22 个章节草稿及 index.md，评估全书结构、章节蓝图完备度、术语与体例一致性，识别技术准确性风险点，形成评估报告成果；不修改草稿正文。
- 输入与依赖：`yocto/` 目录当前草稿；无已登记决定或前置任务。
- 优先级：未设定
- 完成条件与确认方式：产出评估报告（含结构、术语、技术准确性风险三部分清单）并登记成果索引；是否需评审未明确，报告默认 draft，最终确认人为建项用户。
- 进展：2026-09-12 任务登记并完成草稿通读与评估：框架文档（前言/序章/尾声/附录 A/B/C/index.md）全文逐行通读，chapter 1–16 按完整大纲 + 开头段 + 全部 85 处待验证注记 + 定向一致性抽查评估；产出评估报告。
- 成果与验证证据：[A-001 书稿现状评估 v0.1](artifacts/A-001-书稿现状评估.md)（draft）；评估覆盖范围与未覆盖项在报告第 1、7 节如实标注。
- 阻塞与下一位行动人：无；报告中的 D-001 候选（tiger 组实测回填路径）需用户决定，待用户指示是否登记 DECISIONS。
- 更新日期：2026-09-12

## T-003：按章节抽取待校验知识点清单
- 负责人：writer
- 状态：completed
- 授权来源与日期：2026-09-12 用户在本会话直接指派："你按章节把待校验的内容列一个清单"（背景：用户计划自己通读草稿并校验每个知识点）。
- 目标与范围：从 yocto/ 草稿逐章抽取全部待验证注记（HTML 注释台账）与正文可见的待实测标记，整理为按章排序、可逐条勾选的校验清单成果；不修改草稿正文。
- 输入与依赖：yocto/ 当前草稿；A-001 评估结论。
- 优先级：未设定
- 完成条件与确认方式：产出按章组织的校验清单（每条含位置、阻塞级别、待验内容、校验方法）并登记成果索引；报告默认 draft。
- 进展：2026-09-12 任务登记；完成全部 86 处 HTML 注记与正文可见标记的逐条抽取，按章整理为可勾选清单（含级别、位置、校验方法、跨章 X 系列与校验环境提示）。2026-09-12 提交前核对发现与 project-manager 会话存在编号冲突（见下），本任务原编号 T-002 修正为 T-003。
- 成果与验证证据：[A-003 知识点校验清单 v0.1](artifacts/A-003-知识点校验清单.md)（draft；原编号 A-002，2026-09-12 因与 A-002《书稿打磨计划（草案）》重号修正）。
- 阻塞与下一位行动人：无；下一位行动人为用户（持清单通读校验）。
- 更新日期：2026-09-12

## T-002：起草全书打磨计划（阶段、章节安排与范围确认清单）
- 负责人：project-manager
- 状态：completed
- 授权来源与日期：2026-09-12 用户在本会话（project-manager 初始化后）直接指派：项目经理提出盘点书稿并起草打磨计划，用户回复"开始吧"确认。
- 目标与范围：基于 `yocto/` 草稿结构盘点起草《书稿打磨计划》草案，包含阶段划分、章节级修订安排建议、交叉评审与独立审校组织方式建议、里程碑建议，以及需用户确认的范围/验收问题清单；不代替 writer 的书稿现状评估，不修改草稿正文。
- 输入与依赖：`yocto/` 目录草稿结构；T-001 书稿现状评估报告（进行中，产出后用于校准计划）；PROJECT.md 待确认范围与交付要求。
- 优先级：未设定
- 完成条件与确认方式：形成计划草案成果（draft）并登记成果索引；计划批准与范围确认由用户另行决定，不属于本任务完成条件。
- 进展：2026-09-12 完成任务登记、全书结构盘点（22 章标题结构、index.md 引用核查、待验证注记扫描）；T-001 评估报告产出后已将其结论并入草案校准（85 处注记 / 48 处阻塞级、glossary.md 缺失、2 处跨章出入等）；范围问题清单登记为 D-001–D-006（proposed）。
- 成果与验证证据：[A-002 书稿打磨计划（草案）v0.1](artifacts/A-002-book-polishing-plan.md)（draft），已登记成果索引。
- 阻塞与下一位行动人：无；下一步由用户确认 D-001–D-006，随后 project-manager 更新计划 v0.2 并登记 P1/P2 任务。
- 更新日期：2026-09-12

## T-004：修复 workspace/yocto/index.md（补全章节表、修正文件名、处理悬空引用）
- 负责人：writer
- 状态：completed
- 授权来源与日期：2026-09-13 用户在 project-manager 会话确认：项目经理提议将 A-003 X-3 项作为 P1 确定性修复并行安排，用户回复"可以"。
- 目标与范围：修复 `workspace/yocto/index.md`（A-003 X-3 项）：章节表补齐 task16–task21 共 6 行（chapter 15、16、尾声、附录 A/B/C）；修正 task07–11 表内文件名与实际文件的弯引号/下划线失配；处理 `configs/numbering.md`、`outline/yocto/index.md`、`state/yocto/progress.md` 三个悬空引用（移除或替换为仓库内实际资料，处理方式由 writer 提出、用户复核）。仅修改 index.md，不碰章节正文。
- 输入与依赖：A-003 知识点校验清单 X-3 项；书稿当前位于 `projects/book-yocto/workspace/yocto/`（2026-09-16 自 `projects/book-yocto/yocto/` 迁入，内容与行号未变）。与用户通读校验并行，无冲突（用户通读对象为章节正文）。
- 优先级：未设定
- 完成条件与确认方式：index.md 修复完成、表中文件名与实际文件逐一核对一致、无悬空引用；完成后由 project-manager 复核登记，是否需用户确认修复口径（悬空引用的处理选择）视 writer 提案而定。
- 进展：2026-09-13 任务登记，待 writer 会话接收执行。2026-09-19 writer 会话接收并完成修复：章节表补齐 task16–task21 共 6 行；修正 task07–task10 共 4 个失配文件名（表内弯引号 → 实际文件的下划线占位；此前台账与评估写"5 个/task07–11"，逐一核对实为 4 个，task11 文件名本无引号）；`configs/numbering.md` 引用改为内联说明双轨编号约定，`outline/`、`state/` 进度行整行移除（写作工作流资料不在本仓库，目录页不保留悬空指引）；全部 22 个表内文件名与磁盘文件逐一核对一致，无残留悬空引用。
- 成果与验证证据：修复后的 [index.md](workspace/yocto/index.md)（书稿正文，非 artifacts 成果）；核对证据见 2026-09-19 会话的逐文件存在性检查输出。
- 阻塞与下一位行动人：无；下一位行动人 project-manager（按完成条件复核登记）；悬空引用的处理口径（移除而非重建）已在修复中采用，用户复核有异议可回退（Git 可追溯）。
- 更新日期：2026-09-19

## T-005：起草全书体例约定草案
- 负责人：writer
- 状态：completed
- 授权来源与日期：D-005（2026-09-18 用户确认）：由 writer 在 P1 提出体例约定草案，用户确认后作为全书统稿依据。
- 目标与范围：起草《体例约定》草案，覆盖章节标题层级（边界章 H1 与技术章 H2 不一致）、节编号风格（`1.` 带点与 `1` 不带点、附录 `A.1`）、中英文术语书写约定（BitBake 变量、命令、文件路径格式），并处理 A-001 §3.2 已列待统一项（含 2 处"第 N 章"旧式引用、前言 preface tag 缺失的处理建议）；产出为 draft 成果，不直接改章节正文。
- 输入与依赖：A-001 §3.2；A-003；书稿 workspace/yocto/ 当前版本。
- 优先级：未设定
- 完成条件与确认方式：草案登记为成果（draft）并提交用户确认；用户确认后记为统稿依据（更新成果状态）。
- 进展：2026-09-18 任务登记。2026-09-19 writer 会话执行完成：先采集全稿体例事实（标题层级逐章核对、773 个代码块语言标记分布、提示框三级计数、术语变体频次、"第 N 章"逐处核对——真正旧式引用实为 1 处而非 2 处、前言无 tag 标注确认），再起草草案；10 节约定每条含现状与建议，2 处分歧项（章标题层级方案、preface tag 补法）标【请裁决】。
- 成果与验证证据：[A-004 体例约定（草案）v0.1](artifacts/A-004-体例约定草案.md)（draft），已登记成果索引。
- 阻塞与下一位行动人：无。2026-09-19 用户确认草案（章标题层级按方案 A；章节 git tag 事宜由用户自行决定、移出约定），A-004 更新至 v0.2 并转 approved，记为统稿依据（D-005 完成条件满足）。
- 更新日期：2026-09-19

## T-006：chapter 1 试点闭环（修订→交叉评审→复核→试读版）
- 负责人：writer
- 状态：**completed（2026-09-25 用户确认定稿："我看了下原稿也确认OK"；定稿版本 Git 92ede7e，816 行）**
- 授权来源与日期：2026-09-18 project-manager 按 A-002 v0.2（P2 阶段，范围依据 D-001~D-006）在授权范围内安排；计划依据用户当日确认。
- 目标与范围：以 chapter 1《把 Yocto 跑起来》（workspace/yocto/task02-1-把Yocto跑起来.md）走完整章节闭环流程：作者自查与修订 → 任务内交叉评审（reviewer 技术审校；project-manager 项目管理意见）→ 作者统一修订 → 复核 → 试读版。目的是验证书稿生产协作流程本身，为 P3 全面铺开定型节奏与记录方式。
- 输入与依赖：书稿当前版本；A-001、A-003；体例约定草案（T-005）若已确认则作为修订依据，未确认时章节修订暂不涉及体例改动。
- 优先级：未设定
- 完成条件与确认方式：本轮评审意见全部处理并复核通过，产出试读版；按 D-003 书稿维度，章节定稿需用户确认。chapter 1 无 C-W 挂账，不依赖 P0.5。
- 进展：2026-09-18 任务登记。2026-09-19 writer 会话执行作者自查与修订：通读全章（801 行），自查发现 3 类问题并处理——① 标题层级按 A-004 方案 A 整体升一级（章 `##`→`#` 1 处、节 `###`→`##` 8 处、小节 `####`→`###` 20 处，sed 占位符批量替换后 grep 逐行核对，代码块内单 `#` 注释零误伤）；② 417 行旧式引用"那是第三章的事"改为"那是 chapter 3 的事"（全书唯一一处，A-004 §3 已定）；③ 术语与体例逐项核对（poky/Poky、bitbake/BitBake 分工、输出块标题、提示框三级、文件路径注释）均符合 A-004 v0.2，无需改动。修订说明：48 行 🔥 框内 `Do not use Bitbake as root` 为程序输出引用，按约定不改写，但本人未验证真实报错原文，出版前需与真实输出逐字核对（评审时可请 reviewer 留意）。本章零待验证注记，属已实测干净区。修订稿提交评审。2026-09-23 writer 会话完成统一修订：R-1~R-5、U-1~U-16、D-008 本章 24 处改名全部处理（逐条处理回复见下方评审记录）；正文弯引号替换 64 个字符（reviewer 所计 66 处中 2 个在行内代码 `http_proxy = "http://..."` 内，按口径保持 ASCII）；U-3 按"删除 1.1.3、1.6.2 自包含改写"方案执行，待用户随定稿确认；A-004 同步出 v0.3（§7 现状修正 + §4 两条补充条款草案，draft 待确认）。机械改动由脚本 projects/book-yocto/workspace/t006-unified-revision.py 执行（逐规则命中校验 + 残留断言，已随版本入库可追溯）。2026-09-23 writer 会话执行定稿前修订（project-manager 按 T-008 复核结论安排）：按 T-008 核查记录实测证据修复 3 个不符项——E-1：1.2.3 `ls meta-yocto-bsp/` 输出块移除实际不存在的 `recipes-core`，改为实测完整清单（conf、lib、README.hardware.md、recipes-bsp、recipes-graphics、recipes-kernel、wic）；E-2：1.3.3 `show-layers` 输出块按实测原文改写（层名 core/yocto/yoctobsp、104 字符分隔线，字节级取自核查记录），并在输出块后补层集合名与目录名对应说明（`core`=`meta`、`yocto`=`meta-poky`、`yoctobsp`=`meta-yocto-bsp`）；E-3：1.3.1 `ls $BUILDDIR/conf/` 输出块列全实测 5 个文件（补 conf-notes.txt、conf-summary.txt）。逐条处理见下方"T-008 不符项处理"小节。
2026-09-25 writer 会话执行定稿修订（用户在 writer 会话指示"执行 T-006 定稿修订"触发），输入 U-17~U-20、T-009 不符项 N-1/N-2 与观察项 O-1 加注全部处理——U-17：删除 1.2.1 节 git:// 备选写法 💡 整行（本章 git:// 零出现）；U-18：L7 达哥台词"Scarthgap 分支"改为"`scarthgap` 分支"（分支名按代码标识符口径，全章其余 6 处大小写各归其位无需动），分支名规则同步增补为 A-004 v0.3 第三条补充条款草案；U-19：1.3.2 💡 末句改写，拆开"反方向的边界"压缩比喻，补齐"每个并行任务各占内存（C++ 单任务峰值 1~2 GB 量级）→ 任务数×单任务内存超物理内存 → swap/OOM → 整机卡死"因果链，点明"设小"的对象（单任务内存量级口径请 reviewer 复核确认）；U-20：1.4.3 💡 关键文件顺序与文件名形态对齐上方输出示例（`Image` → `.ext4` 全名 → `.qemuboot.conf` 全名）；N-1：登录横幅按用户"以最新版本为准"口径回填实测值 5.0.18→5.0.20，块后补通用漂移 💡（版本号随点版本更新，以本地输出为准）；N-2：1.5.2 `ls -1 $BUILDDIR` 输出块移除 `bitbake.lock`（仅构建运行期存在），清单与 T-009 构建后实测 6 项逐字一致；O-1：任务汇总行同口径回填 4059→4073（断点续跑形态差异"0 didn't need to be rerun"为首次从零形态，保留不变），块后补通用漂移 💡（`all succeeded` 为成功标志）；O-2 为证据边界记录，不涉及书稿改动。修订后全章 816 行，grep 校验 git://、"反方向"、4059、5.0.18 零残留。
- 成果与验证证据：chapter 1 定稿修订版（workspace/yocto/task02-1-把Yocto跑起来.md，816 行，Git 提交见历史；此前版本：统一修订版 9772f9c、定稿前修订版 89ccb72、编辑器格式化版 ff9c794）；修订依据 A-004 v0.2 + v0.3 待确认条款；事实核查来源：T-008 容器实测、T-009 远程实测（workspace/t009-chapter1-build-check.md）、GitHub API（2026-09-23）。
- 阻塞与下一位行动人：无。2026-09-25 闭环：reviewer 有界复核通过（见"定稿微调复核"小节）后，用户通读定稿修订版原稿确认 OK，chapter 1 定稿。随定稿一并闭环：①U-3 删节方案（删除 1.1.3、代理并入 1.6.2 自包含）——原稿即含该方案，确认原稿即确认方案；②reviewer 不阻断观察（L483/L532 两条新增 💡 的 scarthgap 大小写口径不齐）——用户接受现状，留待下次触及该章时顺手统一。**遗留两项开口口径**（不阻塞本章定稿，随 P3 启动前确认）：①A-004 v0.3——**已闭环，2026-09-25 用户批准**（"批准的是 A-004 体例约定 v0.3"），P3 统稿依据生效；②全书列表风格归一——**已闭环，D-009（2026-09-25 用户确认样式一）**：全书统一 `-` 紧凑，约定入 A-004 v0.4，task02 归一由 T-010 执行。
- 更新日期：2026-09-25

### 本轮评审：chapter 1 修订稿（workspace/yocto/task02-1-把Yocto跑起来.md，Git b83a631，第 1 轮）
- 受评正文：workspace/yocto/task02-1-把Yocto跑起来.md（801 行；评审时点工作区干净，即 b83a631 提交内容）
- 安排来源与日期：2026-09-18 project-manager 按 A-002 v0.2（P2 阶段）在授权范围内安排（见 T-006 任务记录）
- 参与成员、各自范围与完成条件：reviewer 技术审校（事实依据、技术陈述、跨章一致性、教学视角，命令/配置/代码准确性核查）；project-manager 项目管理意见；用户通读意见（D-007：建议与疑问，经 project-manager 汇集）。意见齐后 writer 统一修订并复核
- 本轮进度与下一位行动人：reviewer 意见已提交（2026-09-19，结论 revise，R-1~R-5）；project-manager 项目管理意见已提交（2026-09-19，pass，流程维度）。统一修订输入追加：D-008 改名（本章 24 处）；用户通读意见 U-1、U-3~U-15（2026-09-22 起陆续登记；U-15 已闭环 R-5 口径=移除章末打 tag 动作）。chapter 1 通读已于 2026-09-23 完毕（用户在本会话声明"第1章我已经读完了"，通读意见以已登记的 U-1~U-16 为准，不再追加）。R-3 引号口径已确认（2026-09-23 用户在 project-manager 会话确认："代码块应该需要直引号，但是中文应该使用弯引号"——即正文与对话用弯引号“”，代码块/命令/配置内保持 ASCII 直引号不动；A-004 §7 约定维持弯引号不变，其"现状基本已如此"描述由 writer 在统一修订时一并修正；本章 66 处随本轮统一修订替换，全书其余章节随 P3 各章修订轮逐章统一，不做一次性全书批量替换）。U-2/U-11 口径已闭环（2026-09-23 用户提供 Dockerfile 并指示"放到 workspace 合适位置、提交进仓库"：存放于 projects/book-yocto/workspace/docker/dockerfile（2026-09-23 用户指定：workspace 是项目工作目录，yocto/ 仅存书稿，项目资料放 yocto 同级目录），环境事实为用户名 oops、家目录 /home/oops、容器主机名 tiger、Ubuntu 24.04 + 清华镜像源；全章 `<your-username>` 占位与 `/home/akai/...` 字面路径按该环境真实值回填，全书其余章节同口径随 P3 覆盖；writer 统一修订时同步核对 1.1.1 依赖清单与 Dockerfile 包集合的一致性）。统一修订输入已全部到齐。下一位行动人：**writer**（用户进入 writer 会话触发统一修订：R-1~R-5 + U-1~U-16 + D-008 本章改名 24 处）→ reviewer 复核 → 用户确认定稿。2026-09-23 writer 统一修订完成并提交复核（受评正文：workspace/yocto/task02-1-把Yocto跑起来.md，Git 9772f9c，792 行，工作区干净）：R-1~R-5、U-1~U-16、D-008 本章改名 24 处全部处理，逐条处理回复见下；同步产出 A-004 v0.3（§7 现状修正 + §4 补充条款草案，draft 待用户确认）。下一位行动人：**reviewer**（复核本轮统一修订）→ 用户确认定稿（含 U-3 删节方案、A-004 v0.3 两项待确认）。2026-09-23 reviewer 复核完成：R-1~R-5、U-1~U-16 逐项复核通过（证据见两处复核小节，含 T-008 容器实测与 GitHub/源码核查）；T-008 实测另发现 3 个本章不符项 E-1/E-2/E-3（show-layers 层名、meta-yocto-bsp 的 recipes-core、conf/ 清单省略标记），不属本轮评审意见范围，待 project-manager 复核 T-008 记录后转 writer 处理。2026-09-23 project-manager 复核 T-008 核查记录通过并安排：E-1~E-3 由 writer 在定稿确认前修订（T-006 范围内、依据 T-008 实测证据），reviewer 对三处修复做有界复核。下一位行动人：**writer**（修订 E-1~E-3）→ reviewer 有界复核 → **用户**确认定稿（含 U-3 删节方案、A-004 v0.3 两项待确认）。2026-09-23 writer 完成 E-1~E-3 定稿前修订并提交有界复核（受评正文：workspace/yocto/task02-1-把Yocto跑起来.md，Git 89ccb72，798 行，工作区干净；处理明细见下方"T-008 不符项处理"小节）。下一位行动人：**reviewer**（对 E-1~E-3 三处修复做有界复核）→ **用户**确认定稿（含 U-3 删节方案、A-004 v0.3 两项待确认）。2026-09-23 reviewer 有界复核通过：E-1~E-3 与当日容器重跑实测逐字一致，新增说明句事实正确，未引入新问题（明细见"T-008 不符项处理"复核小节）；chapter 1 全部评审问题闭环。下一位行动人：**用户**确认定稿（含 U-3 删节方案、A-004 v0.3 两项待确认）。2026-09-25 用户定稿通读中提出新意见 U-17（1.2.1 节 git:// 备选 💡 提示删除建议，详见用户意见小节）；下一位行动人：用户继续定稿通读，意见到齐后 writer 微调 → reviewer 有界复核 → 用户确认定稿。2026-09-25 用户定稿通读完毕：复查意见共 U-17~U-20 四条（删 git:// 提示、L7 大小写、内存边界句改写、产物提示对齐示例），另有 U-18/U-20 两项疑问已随登记作答；同日用户指示 T-009 由 reviewer 执行（实测结果回填完善书稿），故定稿顺序调整为：reviewer 执行 T-009 → PM 复核核查记录 → writer 一次处理（T-009 不符项 + U-17~U-20 + U-3 删节方案与 A-004 v0.3 确认项）→ reviewer 有界复核 → 用户确认定稿。另：2026-09-25 用户编辑器对 task02 自动格式化（4 处列表 `-`→`*` 松散风格、1.8 节 URL 加 `<>` 包裹；渲染效果不变、正文内容零改动），经用户指示提交，定稿候选版本随之更新；全书列表记号主流为 `-`（336 处）而 task02 现为 `*`（21 处），风格归一口径留待定稿微调时请用户确认。2026-09-25 用户口径："以最新版本为准"——书稿与实测存在版本漂移处，定稿修订时直接回填最新实测值：N-1 登录横幅 Poky 5.0.18→5.0.20、O-1 任务汇总行 4059→4073（T-009 实测，远程服务器 tiger，poky HEAD 77d1feb37e）；uname 行原有"以本地输出为准"注释保留不变，downloads 三个版本号（U-14 回填）经 T-009 实测相符无需动。2026-09-25 writer 定稿修订完成（U-17~U-20 四条复查意见、N-1/O-1 按"以最新版本为准"口径回填 5.0.20 与 4073、N-2 移除 bitbake.lock，A-004 v0.3 增补第三条款"git 分支名属代码标识符"；逐条明细见任务进展），提交复核。下一位行动人：**reviewer**（有界复核本次七处改动，重点确认 U-19 单任务内存量级口径）→ **用户**确认定稿（拍板三项：U-3 删节方案、A-004 v0.3 三条补充条款、全书列表风格归一口径）

#### reviewer 的意见
- 日期、评审依据与未覆盖项：2026-09-19。依据：A-004 体例约定 v0.2（approved）、D-001~D-006、T-006 修订说明；官方来源核查（poky scarthgap 分支 GitHub API 与源码、Yocto 5.0 参考手册变量术语表，2026-09-19）。未覆盖项：①未实机运行任何命令（本会话无 Linux 构建环境），所有来源核查均为静态/资料核查；②输出块中的实测数值（4059 任务数、Poky 5.0.18、内核 6.6.127、磁盘/内存数值等）未复核，属用户通读校验范围；③跨章一致性仅抽查 task04/task05 对本章的 5 处引用，未通读其他章节；④前言/序章未读。
- 结论：revise（R-1/R-2/R-3 需修订，其中 R-3 需先确认口径；R-4/R-5 为低severity项）
- 问题：
  - R-1（中，事实）｜位置：1.2.1 节 L148-160 `ls -1` 输出块｜依据与影响：poky scarthgap 实际顶层还有 `documentation/`、`contrib/`、`README.md`、`README.poky.md`、`README.hardware.md`、`MAINTAINERS.md`、`MEMORIAM`、`SECURITY.md`、`LICENSE*` 等条目（GitHub yoctoproject/poky scarthgap 分支，2026-09-19 核查），该输出块无 `# ... (省略)` 标记，读者照做会看到明显更多的条目，"Poky 顶层目录"认知不完整（`documentation/` 尤其值得一提）｜建议：补省略标记或列全；若只想列目录可改用 `ls -d */`｜原稿责任人：writer
  - R-2（中，技术因果）｜位置：1.6.2 节 L740-753（踩坑 2）｜依据与影响：叙事称"export 代理在 `source oe-init-build-env` 之后，导致环境变量没被 BitBake 继承"。但 `oe-init-build-env` 经 `scripts/oe-buildenv-internal` 末尾段无条件把 `http_proxy`、`https_proxy`、`no_proxy` 等并入 `BB_ENV_PASSTHROUGH_ADDITIONS` 并 export；BitBake 每次启动时按该白名单从当前 Shell 环境过滤（`bitbake/lib/bb/utils.py` `approved_variables()`，poky scarthgap，2026-09-19 核查）。同一 Shell 内先 source 后 export 代理变量，随后运行的 `bitbake` 同样可以拿到代理——先后顺序不构成该故障的机制。按错误模型讲解会误导读者排查真实代理问题｜建议：改写故障成因（例如：代理只设在浏览器/系统图形界面、在另一个终端 export、或写入 `~/.bashrc` 但当前 Shell 未 source），保留"先 export 再构建"的建议；或提供可复现的反例证据推翻本判断｜原稿责任人：writer
  - R-3（中，体例）｜位置：全章正文对话与行文引号（直引号 `"`，共 66 处）｜依据与影响：A-004 §7 约定"对话与正文引号用弯引号"，本章全部为直引号；抽查 task00、task03 同样全为直引号，即 A-004 §7"现状基本已如此"与全稿实际不符——这不是本章独有问题，而是约定的事实基础与执行时点问题｜建议：writer 提请用户确认口径——本章按约定机械替换为弯引号（并入本轮修订），或修订 A-004 §7 的现状描述并明确全书统一执行时点；本章评审结论在口径确认前维持 revise 中的本项｜原稿责任人：writer
  - R-4（低，数字）｜位置：1.5.1 节 L564 💡 提示｜依据与影响：提示称 procps-ng 的 `free -h` 会显示 `234Mi`，但与所引 busybox 输出 232996 KiB 不符（232996 KiB ≈ 227.5 Mi，`free -h` 应显示约 227/228Mi）｜建议：改为与 232996 KiB 一致的数值表述｜原稿责任人：writer
  - R-5（低，待确认）｜位置：1.7 节 L784-792 章末 `git tag chapter1` 指引｜依据与影响：A-004 v0.2 按用户指示移除了"章末 git tag 约定"（该事宜用户自行决定），正文仍教读者打 tag 并用 `git checkout chapter1` 回退，需确认是有意保留（作为读者操作建议）还是随约定移除｜建议：writer 提请用户确认后处理｜原稿责任人：writer
  - 改进建议（不阻断，供 writer 斟酌）：①L337"MACHINE 变量（MACHINE）""Machine 配置（Machine）"首释括号与中文名重复；②L581"tmpfs 容量约为 guest 物理内存的一半"与数值（约可用内存 227Mi 的一半）的表述可更精确；③L99 与 L748 的 `no_proxy` 取值不一致（含/不含 `.local`），建议统一。
- 已核查通过项（结论支撑）：标题层级 1×H1 + 8×H2 + 16×H3 + 0×H4，符合 A-004 §1 方案 A；L417 已改"那是 chapter 3 的事"，无残留旧式"第 N 章"引用；index.md 条目与本章一致；全章零 HTML 待验证注记，与 A-003"已实测干净区"记录一致；71 个代码块全部带语言标记（bash 38 / text 31 / bitbake 2），符合 A-004 §5；输出块标题与括号限定形式符合 §5，提示框三级符合 §6，耗时表述带依据符合 §8；L48 引用的 `Do not use Bitbake as root` 与 scarthgap `meta/classes-global/sanity.bbclass` 原文一致（来源核查通过，实机逐字核对留待 D-001 实测阶段）；`qemuarm64` 机器配置确由 OE-Core `meta/conf/machine/qemuarm64.conf` 提供（GitHub API 核查）；task04/task05 对本章的 5 处引用（show-layers、首次构建任务数、Native recipe、qemu-system-native、downloads 缓存）与本章内容一致。
- 作者处理回复（writer，2026-09-23；统一修订版本 Git 9772f9c）：
  - R-1：已处理。`ls -1` 输出块改为 poky scarthgap 顶层完整清单（21 项，含 `contrib/`、`documentation/`、各 README、LICENSE、MAINTAINERS.md、MEMORIAM、SECURITY.md，2026-09-23 GitHub API 逐名核对，按本章 en_US.UTF-8 locale 排序）；阿凯汇报台词补一句"`documentation/` 是官方文档的源码，其余顶层文件主要是许可和说明"。
  - R-2：已处理。采纳 reviewer 机制结论：故障成因改写为"代理在另一个终端窗口 export，运行 bitbake 的当前 Shell 里没有 `http_proxy`"（reviewer 建议成因之一）；删除"须在 `source oe-init-build-env` 之前 export"的错误模型，白名单段改为如实说明——`oe-init-build-env` 已将 `http_proxy`/`https_proxy`/`no_proxy` 并入 `BB_ENV_PASSTHROUGH_ADDITIONS`，BitBake 每次启动按白名单从当前 Shell 环境读取，与 source 先后无关；"先 export 再构建"的建议保留（同一 Shell 内）。改写后 1.6.2 自包含，吸收原 1.1.3 的代理内容（见 U-3）。
  - R-3：已处理（引号口径 2026-09-23 用户确认）。全章正文与对话直引号机械替换为弯引号，共 64 个字符（reviewer 所计 66 处中其余 2 个在行内代码 `http_proxy = "http://..."` 内，按口径保持 ASCII 不动）；脚本按行配对替换并做奇偶校验与残留断言，代码块/行内代码零改动。A-004 §7"现状基本已如此"描述已在 v0.3 中修正并写明执行时点口径（其余章节随 P3 逐章统一）。
  - R-4：已处理。`234Mi` 改为"约 `228Mi`"（232996 KiB ≈ 227.5 MiB）。
  - R-5：已处理（口径经 U-15 闭环=移除）。章末打 tag 叙事句、`git tag chapter1` 命令块与 💡 checkout 提示整段移除。
  - 改进建议：①L337 `MACHINE` 冗余首释已随 U-8 同族修正（变量不再给重复括号首释）；②L581 tmpfs 表述已精确化为"guest 内核可用内存的一半"；③L99 与 L748 的 `no_proxy` 不一致随 1.1.3 删除（U-3）自然消除，全章仅余 1.6.2 一处。
- 复核：reviewer，2026-09-23，复核版本 Git 9772f9c（792 行，工作区干净；HEAD 5e876db 与该文件 diff 为空）：
  - R-1：解决。输出块 21 项与 GitHub API（2026-09-23，全量 23 项含隐藏 .gitignore/.templateconf，`ls -1` 不显示）集合一致；T-008 容器实测（en_US.UTF-8）输出与书稿逐字一致、顺序一致；L271 汇报台词已补 `documentation/` 说明。
  - R-2：解决。1.6.2 故障成因已改写为"另一个终端窗口 export、当前 Shell 无 `http_proxy`"（L749）；L768 白名单机制说明与 scarthgap `scripts/oe-buildenv-internal` L110-114 源码一致（含 HTTP_PROXY http_proxy HTTPS_PROXY https_proxy NO_PROXY no_proxy）；"先 export 再构建"建议保留于同一 Shell 语境，错误先后顺序模型已删除。
  - R-3：解决。脚本化复核：代码块与行内代码之外正文直引号 0 处；弯引号 32 开 32 闭配对平衡共 64 字符；行内代码 `http_proxy = "http://..."`（L770）保留 ASCII 2 字符，代码块内 ASCII 不动，符合 2026-09-23 用户确认口径。
  - R-4：解决。L571 已改"约 `228Mi`"，232996 KiB ≈ 227.5 MiB 换算正确。
  - R-5：解决。章末打 tag 叙事句、`git tag chapter1` 命令块与 checkout 💡 整段移除，全章 grep "git tag" 零残留。
  - 三条改进建议：①冗余首释已消除（现 L345）；②tmpfs 表述已精确化为"guest 内核可用内存的一半"（L588）；③`no_proxy` 全章仅余 L755 一处，取值不一致已随 1.1.3 删除消除。
  - 结论：R-1~R-5 全部解决，复核通过；R-1/R-2 有 2026-09-23 GitHub API、源码与容器实测三重证据。U 系列复核见用户意见小节；T-008 容器实测新发现的 E-1/E-2/E-3 不属本轮评审意见范围，登记于 T-008。

#### project-manager 的意见
- 日期、评审依据与未覆盖项：2026-09-19。依据：T-006 任务记录与修订说明、A-002 v0.2、A-004 v0.2（approved）、D-001~D-006；核对 b83a631 提交 diff——改动为标题层级 1+8+20 处升级与 L417 旧式引用修正，与修订说明一致，改动未超出声明范围。未覆盖项：未逐行通读 chapter 1 正文，技术事实判断以 reviewer 意见为准；本意见限项目管理视角。
- 结论：pass（流程与记录维度；不覆盖 reviewer 的技术结论 revise，本轮综合结论以问题处理结果为准）
- 问题：无（项目管理维度无新增问题）。意见与安排：
  - 流程合规：作者自查修订→提交明确版本（b83a631，工作区干净）→评审者各自署名记录，符合 PROJECT_PROTOCOL 第 4.1 节；意见到齐前 writer 不动受评正文。
  - R-3（引号口径）与 R-5（章末 git tag 指引去留）属需用户确认的口径问题，按 2026-09-13 通读期协作约定由 project-manager 汇集转达用户；writer 在统一修订前取得口径再动笔。
  - 覆盖核对：reviewer 意见覆盖事实、技术因果、体例、数字四类；其未覆盖项②（输出块实测数值）属用户通读校验范围（D-003 学习维度），不构成评审缺口；R-2 涉及机制结论反转风险，writer 处理回复中需给出改写依据或反例证据。
  - 下一位行动人：用户确认 R-3/R-5 口径 → writer 统一修订 → reviewer 复核。
- 作者处理回复：暂无（PM 意见无需逐条处理，口径项随 R-3/R-5 处理）
- 复核：随 R-3/R-5 一并在复核阶段确认

#### 用户的通读意见（D-007）
- 日期与方式：2026-09-22 起，用户通读受评版本 b83a631，意见经 project-manager 会话汇集登记
- 意见：
  - U-1｜位置：1.1.1 节 L27-30 系统依赖安装命令块｜内容：多行续行（`\`）的单条 `sudo apt install` 格式不喜欢，要求改为每包一行的独立命令（`sudo apt install -y gawk` / `sudo apt install -y wget` / …），且各行按包名字母顺序排序；包集合不变｜处理：待 writer 统一修订时执行
  - U-2｜位置：1.1.1 节 L39-46 `whoami` 输出示例（连带 L296、L307 的 `/home/akai/...`）｜内容：用户疑问——示例输出中的用户名 `akai` 写法是否合适；project-manager 核查：全书无 "oops" 字样；本章存在体例并存——L45/L296/L307 用字面 `akai`，L388 起改用 `<your-username>` 占位（L415 有提示说明），两种体例宜统一｜处理：已闭环（2026-09-23 随 U-11 Dockerfile 口径）：示例用户名以 Docker 环境真实用户 `oops` 为准，字面 `akai` 路径与 `<your-username>` 占位统一替换；writer 统一修订时执行
  - U-3｜位置：1.1.3 节"网络代理配置（按需）"（L91-112）｜内容：用户感觉这一小节不太需要｜project-manager 核查：1.1.3 被本章 1.6.2 踩坑 2（代理未传入 BitBake，L725-769）回指依赖，reviewer R-2 又指出 1.6.2 的故障机制讲解有误需改写——两者同涉"代理"主线｜处理：writer 统一修订时一并提出处置方案（删除 1.1.3 并改写/精简 1.6.2，或保留最小提示），方案随试读版请用户确认
  - U-4｜位置：1.2.1 节 L124 clone 命令｜内容：`git clone -b scarthgap git://git.yoctoproject.org/poky.git` 的协议改为直接使用 https 开头（`https://git.yoctoproject.org/poky.git`）｜project-manager 核查：L127 💡 提示现以 https 为 git:// 的备选写法，主命令改 https 后该提示需同步翻转口径（https 为默认、git:// 为可选）｜处理：待 writer 统一修订时执行
  - U-5｜位置：1.2.3 节 L265 达哥（原老周，随 D-008 改名）台词｜内容：用户感觉"你把 `meta/`、`meta-poky/`、`meta-yocto-bsp/` 这三个都叫 **层（Layer）**……"不通顺｜project-manager 核查：别脚点在"你把……都叫"的主语错位——术语是达哥在定义，并非阿凯先这么称呼；且破折号后同位语"组织元数据的目录结构，可以叠加复用"与前句衔接生硬｜处理：writer 统一修订时润色该句（如改为"这三个都叫……"句式），保持术语首释功能不变
  - U-6｜位置：1.3 节 L284 🔥 提示（`source` vs `./` 执行 oe-init-build-env）｜内容：用户感觉"用子进程执行的话这些改动不会传回来"讲得不清楚｜project-manager 核查：技术要点本身正确（子进程修改的环境变量不影响父 Shell），但"传回来"的对象不明——没说清是"不会反映到当前 Shell"，也缺少可观察后果（`./` 执行后当前 Shell 的 `PATH` 里没有 bitbake，后续命令直接找不到）｜处理：writer 统一修订时改写该句，点明影响对象与可观察后果
  - U-7｜位置：1.3.2 节 L327 叙事句｜内容：用户感觉"阿凯打开 `local.conf`，发现默认的 `MACHINE` 是 `qemux86-64`。他想起 tiger 是 ARM 的，但本章目标是 `qemuarm64`，于是跑来问老周"不清楚｜project-manager 核查：逻辑关系混乱——"但"字前后并非对立（qemuarm64 本身也是 ARM64），阿凯真正的纠结（该不该直接设成 tiger-aarch64）要靠下一句台词才补上；"想起 tiger 是 ARM 的"与决策的关系也没说透｜处理：writer 统一修订时改写，让阿凯的疑问在叙事里直接可见（默认 x86 不对→想到自家 tiger→但本章先用官方 qemuarm64 练手，拿不准来问），与后续对话衔接
  - U-8｜位置：1.3.2 节 L341-347 变量释义列表（通病，需全章排查同类）｜内容：用户发现格式不统一的通病——同一段列表里有的用粗体中文名首释（**下载目录（DL_DIR）**），有的只用行内代码块（`BB_NUMBER_THREADS`）；且括号内英文与实际变量对不上（**Sstate 缓存（Sstate）** 的括号既不是中文对照也不是真实变量 `SSTATE_DIR`，大小写也对不上）｜project-manager 核查：违反 A-004 §4（approved）两条规则——①首释应"中文（English）"且后文用中文名，本条列表两种格式混用；②变量名应英文原样加反引号、不改大小写，"（Sstate）"四不像。A-004 未明确"变量是否给中文名首释"的细则，存在约定空白；reviewer 改进建议①（L337"MACHINE 变量（MACHINE）"重复）属同一家族｜处理：writer 统一修订时按 A-004 §4 统一该列表及全章同类处，并就"变量首释格式"提出 A-004 补充条款草案，请用户确认后生效
  - U-9｜位置：1.3.2 节 L339 叙事句（连带 L351 代码注释同一句式）｜内容：用户感觉"老周让他在原有 `local.conf` 末尾追加或修改以下几项"中"追加或修改"很怪｜project-manager 核查：别脚点在两个动作被揉在一起且与位置矛盾——"追加"才发生在末尾，"修改"发生在变量已存在的原行（如 MACHINE 默认行在文件中部且被注释）；L352 注释其实已把两种情况说对（MACHINE 行改默认值，其余已存在则取消注释并修改），L339/L351 的表述却含糊｜处理：writer 统一修订时拆开两种情况表述（已存在的行原地修改/取消注释，不存在的在末尾追加），L339 与 L351 同步
  - U-10｜位置：1.3.2 节 L377 💡 提示（BB_NUMBER_THREADS / PARALLEL_MAKE 取值建议）｜内容：用户没看懂"设成你 CPU 物理核心数或稍多一点"，疑问是否应为"稍少一点"｜project-manager 核查："稍多一点"是常见经验口径（编译含大量 I/O 等待，任务数略多于核心数可填满 CPU 空隙），原文方向大概率有意为之，但**缺少理由**，读者无法判断方向；后半句"内存不足宁可设小"是另一方向的边界，两者并存更需解释｜处理：writer 统一修订时保留"稍多一点"方向并补上理由；如 writer 认为口径本身需复核，交 reviewer 确认后定稿
  - U-11｜位置：1.4 节 L386-392 bblayers.conf 示例块（连带全章 `<your-username>` 占位写法与 U-2）｜内容：用户指示——示例中的 `<your-username>` 占位替换为实际环境目录；用户计划通过 Dockerfile 固化环境｜project-manager 核查：这同时解决了 U-2 的体例并存疑问——Docker 固化环境后，示例写具体路径即为读者真实所见，占位写法失去存在必要；全书其余章节的 `<your-username>` 同类占位应同口径处理（P3 各章修订时覆盖）。待明确项：Dockerfile 的用户名与路径取值（建议与叙事一致用 `/home/akai/workspace`）、Dockerfile 由谁编写及存放位置（建议作为随书资料纳入仓库）｜处理：已闭环（2026-09-23 用户提供 Dockerfile，存 projects/book-yocto/workspace/docker/dockerfile 并入库（位置为用户指定：项目资料放 yocto/ 同级））：用户名 oops、家目录 /home/oops、主机名 tiger；writer 统一修订时本章按真实环境值执行，全书其余章节同口径随 P3 覆盖，全书口径记入 A-004 补充或后续决定
  - U-12｜位置：1.3.3 节 L417 达哥台词｜内容：用户感觉"本章先不讲层优先级和冲突"中"优先级和冲突"出现得唐突｜project-manager 核查：两个概念此前全章未曾出现，读者没有附着点；但紧邻的 show-layers 输出（L410-412）每行末尾的数字列正是层优先级——台词若锚住这个可见细节（如"每行最后那个数字就是优先级"）再预告 chapter 3，就不唐突反而成伏笔｜处理：writer 统一修订时改写该句，锚定输出中的优先级数字列
  - U-13｜位置：全章"Sstate"概念名大小写（L342、L362、L454、L668 共 4 处；通病与 U-8 同族）｜内容：用户感觉"Sstate 缓存"这种名称大小写有问题、看着怪｜project-manager 核查：Yocto 官方写法为 shared state（sstate），概念名约定俗成小写"sstate"；`SSTATE_DIR`、`sstate-cache`、`sstate.bbclass` 等变量/目录/类名按 A-004 §4 原样保留反引号写法没有问题——要改的是行文中的概念名"Sstate"｜处理：writer 统一修订时概念名统一为"sstate 缓存"（首释"共享状态缓存（sstate）"），并纳入 U-8 的 A-004 补充条款（概念名大小写遵循官方写法）
  - U-14｜位置：1.5.2 节 L637-641 downloads 目录输出示例（全章同类 `<版本号>` 占位一并排查）｜内容：用户认为 `busybox-<版本号>.tar.bz2` 这类版本号占位符不好，要求直接使用真实环境的版本号｜project-manager 核查：与 U-11（Dockerfile 固化环境）同向——环境固化后示例即可写真实版本；注意 scarthgap 分支会移动，要版本号稳定需 Dockerfile 固定 poky 的具体提交（pin commit），取值随 D-001 实测回填时从固化环境抄录｜处理：writer 统一修订时先将本节改为真实版本号（若 Dockerfile 尚未就绪则标记随 U-11 环境就位后回填），全书同类占位同口径
  - U-15｜位置：1.7 节 L784-792 章末打 tag 动作（叙事句 + `git tag chapter1` 命令块 + 💡 checkout 提示）｜内容：用户指示——把打 tag 的动作去掉｜**此条同时闭环 reviewer R-5 的口径问题（移除）**；与 A-004 v0.2"章节 git tag 事宜不纳入约定"一致｜全书联动影响（待用户确认范围）：前言"每章末尾都有 git tag"的读法承诺、附录 B/C 的 tag 归属总表、其余各章章末的 git tag 动作——若全书统一去除需一并修订；当前先按本章范围登记｜处理：writer 统一修订时移除本章 L784-792 相关内容；全书范围待用户确认后由 project-manager 登记批量任务
  - U-16｜位置：1.8 节 L798-801 延伸阅读四条官方链接｜内容：用户要求对 Yocto 官方链接做实际检查｜project-manager 核查：2026-09-22 以 `curl -L` 实测四条链接 HTTP 状态与页面 `<title>`，全部 200 且标题与文中描述相符——①`docs.yoctoproject.org/5.0/brief-yoctoprojectqs/index.html` → "Yocto Project Quick Build — The Yocto Project 5.0 documentation"；②`/5.0/ref-manual/variables.html` → "12 Variables Glossary"；③`/5.0/ref-manual/structure.html` → "4 Source Directory Structure"；④`/bitbake/2.8/` → "BitBake User Manual — Bitbake 2.8 documentation"。URL 中版本号（5.0 / 2.8）与本书目标版本（scarthgap 5.0、BitBake 2.8）一致｜处理：本章无需修订；全书其余章节的延伸阅读链接未逐个实测，建议 P3 各章修订时按同法（curl 状态码 + 标题核对）逐章核查，由 project-manager 在相应任务中登记要求
  - U-17｜位置：1.2.1 节 L124 💡 提示（git:// 备选写法；定稿候选版 Git 89ccb72 的复查通读意见，2026-09-25）｜内容：用户感觉"如果公司网络只允许 `git://` 协议出口，可以把地址换回 `git://git.yoctoproject.org/poky.git`。两种协议最终拿到的代码完全一致"这条提示没必要加｜project-manager 核查：该提示是 U-4 口径翻转的残留——受评版主命令用 git://、https 为备选；U-4 后主命令已改 https，提示翻转为 git:// 备选。但其前提"公司网络只允许 git:// 出口"现实中几乎不存在：git:// 走未加密的 9418 端口，恰是企业防火墙最常封禁的，https（443）才是普遍放行的——真实的高频故障（git:// 被封）已由"https 作默认主命令"无声覆盖，无须行文；T-008 容器实测 https 克隆成功（1m48s）亦佐证默认路径可用。删除后本章 git:// 零出现，无回指依赖，"两种协议代码一致"这一事实随提示一并消失无损失。用户直觉成立，建议删除整行｜全书联动（非本章范围）：task07 L111、task14 L638-662 的 CI 脚本仍以 git:// clone poky/meta-arm，与 U-4 口径不一，随各章 P3 修订轮统一为 https，由 project-manager 在相应任务登记｜处理：待 writer 定稿微调时删除 L124 整行提示（用户定稿意见如仅此一条，微调 + reviewer 有界复核后即定稿）
  - U-18｜位置：全章 scarthgap 大小写（本章 7 处：L7、L24、L118、L121、L137、L542、L790；定稿候选版 Git 89ccb72 复查通读意见，2026-09-25）｜内容：用户疑问——git clone 的分支是 `scarthgap`，但文中某些地方写 Scarthgap，大小写混用有关系吗｜project-manager 核查：**技术上无影响**——命令与输出中的 `scarthgap` 小写就是分支本名（L121 命令、L137 `git branch` 输出均正确），不会因大小写出错。**体例上可理顺**，规则与 A-004 已有约定同族：指 git 分支（代码标识符）→ 反引号+小写 `scarthgap`（§4 原样大小写）；指发行版/版本名（概念名）→ Scarthgap 大写（官方写法，docs.yoctoproject.org 作 "Yocto Project 5.0 Scarthgap"；与 U-13 的"概念名大小写遵循官方写法"补充条款同族）。本章逐处核对：L121/L137/L118（分支）与 L24/L542/L790（发行版）均各归其位 ✅；**唯一口径不齐的是 L7 达哥台词"Scarthgap 分支"——指分支却用了发行版大写**，建议改为"`scarthgap` 分支"｜全书为面级现象：Scarthgap 54 处（15 文件）+ scarthgap 31 处，随各章 P3 修订轮按同规则逐章统一；建议 A-004 v0.3 §4 补充条款增列"git 分支名属代码标识符，行文引用用反引号+原样大小写"｜处理：writer 定稿微调时改 L7 一处；A-004 条款增补随 v0.3 一并请用户确认
  - U-19｜位置：1.3.2 节 L397 💡 提示末句（"反方向的边界是内存……"；U-10 改写后的文本；定稿候选版 Git 89ccb72 复查通读意见，2026-09-25）｜内容：用户读"反方向的边界是内存：内存不足时宁可设小一点，否则并行编译会把机器卡死"有疑惑，判断读者也会有｜project-manager 核查：三处疙瘩——①"反方向的边界是内存"比喻压缩过度："反方向"需读者回溯"设多一点"的方向，"边界是内存"把方向比喻与约束条件揉在一起（内存不是边界，内存不足才是触发条件）；②因果链断环：没说清任务数与内存的关系——关键是**每个并行编译任务都占内存**（C++ 单文件编译峰值可达 1~2 GB 量级），任务数 × 单任务内存超过物理内存时系统 swap 甚至 OOM，外在表现才是"卡死"，缺了这环"否则卡死"只是断言；③"宁可设小一点"对象不明（设小哪个/相对于什么——相对于按核心数设的值）｜处理：writer 定稿微调时改写末句，把"任务数上限由内存决定"的因果链补出（每个并行任务吃内存→超额→swap/OOM→整机卡死），拆开压缩比喻、点明"设小"的对象；技术口径（单任务内存量级、经验法则）如有不确定交 reviewer 确认
  - U-20｜位置：1.4 节 L503 💡 提示（deploy 产物关键文件）与 L494-501 输出示例（定稿候选版 Git 89ccb72 复查通读意见，2026-09-25）｜内容：用户指示——此处需与前面输出示例在内容与先后顺序上保持一致｜project-manager 核查：两处不一致——①顺序：示例按 `Image` → `ext4` → `qemuboot.conf` → `manifest` 列出，提示却以 ext4 → `Image` → qemuboot 为序；②文件名形态：示例中 qemuboot.conf 为全名 `core-image-minimal-qemuarm64.rootfs-<时间戳>.qemuboot.conf`，提示只写后缀 `.qemuboot.conf`，而同句 ext4 又带 `<时间戳>` 全名，两种形态混用｜处理：writer 定稿微调时按示例统一——关键文件顺序改为 `Image`（内核镜像）→ `core-image-minimal-qemuarm64.rootfs-<时间戳>.ext4`（根文件系统镜像）→ `core-image-minimal-qemuarm64.rootfs-<时间戳>.qemuboot.conf`（runqemu 启动配置），文件名形态与示例逐字一致
- （受评版 b83a631 通读 2026-09-23 已完毕；U-17 起为定稿候选版 Git 89ccb72 的复查意见，随用户定稿通读追加）
- 作者处理回复（writer，2026-09-23；统一修订版本 Git 9772f9c）：
  - U-1：已处理。改为 `sudo apt update` + 每包一行独立 `sudo apt install -y <包名>`，24 个包按包名字母序排列，包集合不变；配套叙事"一口气装上"改为"照着清单逐个装上"。
  - U-2：已处理（随 U-11 口径）。`whoami` 输出 `akai`→`oops`，L296/L307 两处 `/home/akai/...`→`/home/oops/...`；全章字面 `akai` 与 `<your-username>` 占位清零（脚本残留断言通过），统一为固化环境真实值。
  - U-3：已处理（处置方案：删除 1.1.3，代理内容并入 1.6.2 自包含改写——随 R-2 一并完成）。1.1.3 为 1.1 最后一节，删除后无节号变动；全书 grep 无其他章节引用 1.1.3，本章末尾指向"踩坑 2"的 ⚠️ 随节一并移除。方案按约定随试读版请你确认。
  - U-4：已处理。clone 主命令改为 `https://git.yoctoproject.org/poky.git`；💡 提示口径同步翻转（`git://` 为备选写法）。
  - U-5：已处理。改为"`meta/`、`meta-poky/`、`meta-yocto-bsp/` 这三个都是 **层（Layer）**：组织元数据的目录结构，可以叠加复用"，主语错位与破折号衔接问题解决，术语首释功能保留。
  - U-6：已处理。🔥 改写点明影响对象与可观察后果：子进程对环境变量的改动不会反映回当前 Shell，结果是当前 Shell 的 `PATH` 里没有 `bitbake`，下一条命令直接提示找不到。
  - U-7：已处理。叙事改写为"默认 `qemux86-64`（x86）跟 tiger 的 ARM 对不上→想改 tiger 机器名→但本章说好先用官方 `qemuarm64`→拿不准来问"，疑问在叙事里直接可见，与后续台词衔接。
  - U-8：已处理。变量释义列表统一为 **中文名（`变量`）** 格式；全章同族一并修正：L337 `MACHINE` 冗余首释、`local.conf`/`bblayers.conf` 冗余括号、`（BBLAYERS）`、`（bitbake-layers）`、`（core-image-minimal）`、`（oe-init-build-env）` 括号内补反引号。"变量首释格式"补充条款草案已写入 A-004 v0.3 §4，待你确认后生效。
  - U-9：已处理。叙事句与 local.conf 注释同步改为"已存在的变量在原行修改/取消注释，没有的追加到文件末尾"，追加与修改两个动作拆开，与 L360 注释的实际情况一致。
  - U-10：已处理。保留"稍多一点"方向并补理由（编译大量时间等磁盘/网络 I/O，任务数略多于核心数可填满 CPU 空隙），内存不足设小作为反方向边界单独说明。
  - U-11：已处理。`bblayers.conf` 示例与 show-layers 输出全部回填 `/home/oops` 真实路径（输出块按真实列宽重新对齐）；原占位 💡 改写为"示例取自配套固化构建环境（用户 `oops`、家目录 `/home/oops`、主机名 `tiger`）"。同步核对：1.1.1 依赖清单与 Dockerfile 包集合无冲突——Dockerfile 提供基础环境（build-essential/git/wget/locales/python3 等），本章清单为 Yocto 构建必需超集（gawk、diffstat、texinfo、chrpath 等由其补充），pip 经 Dockerfile get-pip 与本章 `python3-pip` 并存不矛盾。全书其余章节同口径随 P3 覆盖。
  - U-12：已处理。台词锚定 show-layers 输出每行末尾的数字列——"注意每行最后那列数字——那就是层的优先级"，再预告 chapter 3。
  - U-13：已处理。4 处概念名"Sstate"统一为"sstate"（首释"共享状态缓存（sstate）"于 1.3.2 变量列表，1.5.2 处改为引用"1.3.2 说过的 sstate 缓存"）；`SSTATE_DIR`、`sstate-cache` 等代码写法不动。概念名大小写规则已纳入 A-004 v0.3 §4 补充条款草案。
  - U-14：已处理。downloads 示例改为真实版本号 `busybox-1.36.1.tar.bz2`、`curl-8.7.1.tar.xz`、`gcc-13.4.0.tar.xz`（2026-09-23 poky scarthgap 配方核查：busybox_1.36.1.bb、curl_8.7.1.bb、gcc-13.4.inc PV=13.4.0）；保留"版本号随上游更新而变化"的说明，D-001 实测回填时再从固化环境逐字核对。`<时间戳>` 为每次构建必然不同的值，非版本号占位，保留原占位及其 💡 说明。
  - U-15：已处理。章末打 tag 叙事句、命令块、checkout 💡 整段移除（同 R-5）。全书联动项（前言读法承诺、附录 B/C tag 归属表、其余各章章末动作）待你确认范围后由 project-manager 登记批量任务。
  - U-16：无需修订（project-manager 2026-09-22 实测四条链接 HTTP 200 且标题相符）。其余章节链接的逐章核查建议请 project-manager 在 P3 任务中登记。
- 复核：reviewer，2026-09-23，复核版本 Git 9772f9c（792 行）。逐项结果：
  - U-1：解决。24 包逐条独立命令、按包名字母序（程序化核验 sorted 通过）；包集合与受评版 b83a631 完全一致（24/24 无增删）；T-008 容器内逐条安装全部 rc=0。
  - U-2：解决。`whoami` 输出 akai→oops，全章字面 akai 由 4 处清零（grep 复核）；T-008 实测 whoami=oops、HOME=/home/oops、hostname=tiger，三者一致。
  - U-3：解决（删节方案按约定待用户随定稿确认）。1.1.3 已删除，全仓 grep "1.1.3" 零残留；1.6.2 自包含改写完成（机制正确性见 R-2 复核）。
  - U-4：解决。clone 主命令已改 https（L121），💡 口径翻转为 git:// 备选（L124）；T-008 实测该 https 地址克隆成功（1m48s）。
  - U-5：解决。L273 改为"这三个都是 **层（Layer）**：组织元数据的目录结构，可以叠加复用"，主语错位与衔接问题消除，术语首释功能保留。
  - U-6：解决。L292 已点明影响对象（当前 Shell）与可观察后果（`PATH` 里没有 `bitbake`，命令找不到）。
  - U-7：解决。L335 叙事改为"默认 `qemux86-64` 与 tiger 的 ARM 对不上→想改 tiger 机器名→本章先用官方 `qemuarm64`→拿不准来问"，疑问在叙事内可见，与后续台词衔接。
  - U-8：解决。L349-354 变量列表统一为 **中文名（`变量`）**；全章 grep 无（MACHINE）（BBLAYERS）（Sstate）等旧式冗余/失配括号残留；A-004 v0.3 §4"首释中代码性标识加反引号"补充条款草案已就位（待用户确认）。
  - U-9：解决。L347 叙事与 L359-360 注释已拆分"原行修改/取消注释"与"末尾追加"两种情况。
  - U-10：解决。L385 保留"稍多一点"并补磁盘/网络 I/O 理由，内存反向边界单独说明。
  - U-11：占位回填部分解决。bblayers.conf 示例与 show-layers 路径已回填 `/home/oops`（T-008 实测 bblayers.conf 逐字一致）；但"输出块按真实列宽重新对齐"的声明经 T-008 实测不成立——show-layers 真实输出层名为 `core/yocto/yoctobsp`、列宽亦不同，已登记为 T-008 不符项 E-2 待 writer 处理。U-11 本项（`<your-username>` 占位清零，grep 复核 8→0）复核通过。
  - U-12：解决。L424 台词已锚定"每行最后那列数字"；T-008 实测优先级列均为 5，锚点成立。
  - U-13：解决。全章"Sstate"零残留（grep 复核）；首释"共享状态缓存（sstate）"在 L350，L675 改为引用 1.3.2；`SSTATE_DIR` 等代码写法未动；A-004 v0.3 §4 概念名大小写条款草案已就位（待用户确认）。
  - U-14：解决。busybox-1.36.1 / curl-8.7.1 / gcc-13.4.0 与 poky scarthgap 配方逐一相符（2026-09-23 GitHub 核查：busybox_1.36.1.bb、curl_8.7.1.bb、gcc-13.4.inc PV="13.4.0"）。
  - U-15：解决。章末 tag 动作整段移除（证据同 R-5 复核）。
  - U-16：无需修订，复核确认。2026-09-23 reviewer 复测四条链接均 HTTP 200，页面 `<title>` 与文中描述相符（Quick Build / Variables Glossary / Source Directory Structure / BitBake User Manual 2.8）。
  - 结论：U-1~U-16 全部按处理回复落实，复核通过；U-3 删节方案与 A-004 v0.3 两项补充条款按约定留待用户定稿时确认；U-11 附 T-008 E-2 遗留（不属 U-11 本身）。

#### T-008 不符项处理（E-1~E-3，定稿前修订）
- 安排来源与日期：2026-09-23 project-manager 复核 T-008 核查记录通过后安排——E-1~E-3 由 writer 在定稿确认前修订（T-006 范围内、依据 T-008 实测证据），reviewer 对三处修复做有界复核
- 修订版本：Git 89ccb72（798 行，工作区干净）；证据来源：[T-008 核查记录](workspace/t008-chapter1-env-check.md) 第二节实测原文
- 逐条处理（writer，2026-09-23）：
  - E-1：已修复。1.2.3 节 `ls meta-yocto-bsp/` 输出块移除实际不存在的 `recipes-core`，按实测改为完整清单 7 项（conf、lib、README.hardware.md、recipes-bsp、recipes-graphics、recipes-kernel、wic），列全后不再需要省略标记。
  - E-2：已修复。1.3.3 节 `show-layers` 输出块按核查记录实测原文逐字改写（层名 `core`/`yocto`/`yoctobsp`，104 字符分隔线与列宽，脚本字节级取自记录）；输出块后新增一句层集合名与目录名对应说明（`core` 即 `meta`，`yocto` 即 `meta-poky`，`yoctobsp` 即 `meta-yocto-bsp`）。U-12 台词锚点（每行末尾数字列=优先级）与 U-11 路径回填均不受影响（优先级均为 5、路径不变）。
  - E-3：已修复。1.3.1 节 `ls $BUILDDIR/conf/` 输出块列全实测 5 个文件（补 conf-notes.txt、conf-summary.txt），按字母序，不再缺项亦无需省略标记。
- 复核：reviewer，2026-09-23，复核版本 Git 89ccb72（798 行，工作区干净；HEAD 53640a9 与该文件 diff 为空）。范围限 E-1~E-3 三处输出块及新增对应说明：
  - E-1：落实。书稿块与当日容器重跑 `ls meta-yocto-bsp/` 输出逐字一致（conf、lib、README.hardware.md、recipes-bsp、recipes-graphics、recipes-kernel、wic，7 项完整、为 en_US.UTF-8 实测序）；列全后移除省略标记正确。
  - E-2：落实。书稿块与当日容器重跑 `bitbake-layers show-layers` 输出逐行等长且逐字一致（102/104/95/95/95 字符）；新增说明句事实正确——层名取自各层 `conf/layer.conf` 的 BBFILE_COLLECTIONS（实测 meta→"core"、meta-poky→"yocto"、meta-yocto-bsp→"yoctobsp"）；U-12 优先级锚点（末列数字均为 5）与 U-11 路径回填不受影响，确认成立。
  - E-3：落实。书稿块与当日容器 `ls $BUILDDIR/conf/` 输出逐字一致（bblayers.conf、conf-notes.txt、conf-summary.txt、local.conf、templateconf.cfg 共 5 项完整），无省略标记正确。
  - 新问题排查：89ccb72 书稿 diff 仅触及上述三处（13 增 7 删）；全章引号复查正文直引号 0 处、弯引号 32 开 32 闭平衡；1.2.3 其余输出块省略标记保留完好；未引入新问题。
  - 结论：E-1~E-3 修复全部落实，有界复核通过。chapter 1 第 1 轮评审全部问题（R-1~R-5、U-1~U-16、E-1~E-3）均已处理并复核闭环，可提交用户确认定稿（U-3 删节方案与 A-004 v0.3 两项补充条款按约定随定稿确认）。

#### 定稿微调复核（U-17~U-20、N-1/N-2、O-1，reviewer 有界复核）
- 复核人与日期：reviewer，2026-09-25。复核版本：Git 92ede7e（816 行，工作区干净；chapter 1 相对 89ccb72 的内容改动即本次定稿微调，ff9c794 为编辑器格式化、渲染不变）。
- 复核范围与依据：限 U-17~U-20、N-1/N-2、O-1 七处改动；依据 T-006 登记的处理口径、T-009 核查记录实测值、2026-09-25 用户口径（版本漂移以最新实测值为准）。
- 逐项结果：
  - U-17：落实。git:// 备选提示整行删除，全章 grep `git://` 零残留。
  - U-18：落实。L7 已改 `` `scarthgap` 分支 ``；全章 8 处复查：分支语境（L7/L118/L121/L135）小写、发行版语境（L24/L532/L544/L791）Scarthgap，各归其位。
  - U-19：落实，技术口径确认通过。改写后因果链完整（每个并行任务各占内存→任务数×单任务内存超物理内存→swap/OOM→整机卡死），"设小"对象已点明（比核心数还小）；"编译大型 C++ 文件单任务峰值 1~2 GB 量级"的表述为带"可达/量级"上限的保守口径，与 GCC 编译大型 C++ 翻译单元的实际内存行为相符，可作为经验法则成立。
  - U-20：落实。💡 关键文件顺序与文件名形态与上方输出示例逐字对齐（`Image` → `.ext4` 全名 → `.qemuboot.conf` 全名）。
  - N-1：落实。横幅回填实测值 5.0.20（与 T-009 实测逐字一致），块后新增漂移 💡，与同页 uname 行注记口径一致。
  - N-2：落实。`bitbake.lock` 已移除，输出块 6 项与 T-009 构建完成后实测清单逐字一致。
  - O-1：落实。任务汇总行回填 4073（与 T-009/T-008 两处实测互证），保留首次从零形态 `0 didn't need to be rerun`（与本章首次构建叙事一致），块后新增漂移 💡 并点明 `all succeeded` 为成功标志；与 1.4.1"4000 多个任务"表述不冲突。
- 新问题排查：全章正文（剔除代码块与行内代码）直引号 0 处、弯引号 32 开 32 闭配对平衡；`git://`、`4059`、`5.0.18`、"反方向"零残留；两条新增 💡 的级别用法符合 A-004 §6。
- 改进观察（不阻断，不本轮处理）：1.4.3 新增漂移 💡（L483）行文用 bare 小写 `scarthgap`，而 1.5.1 新增 💡（L532）用 Scarthgap——同批新增的两处注记口径不齐（按 U-18 规则，"点版本更新"指发行版系列时宜用 Scarthgap），建议下次触及该章时顺手统一。
- 结论：七处改动全部落实，复核通过，未引入新问题。chapter 1 评审与实测核查问题至此全部闭环。下一位行动人：**用户**确认定稿，定稿时三项一并拍板：①U-3 删节方案；②A-004 v0.3（§7 修正 + §4 三条补充条款）；③全书列表风格归一口径。

## T-007：全书人物"老周"更名"达哥"（chapter 1 以外章节）
- 负责人：writer
- 状态：completed（2026-09-25 project-manager 复核通过）
- 授权来源与日期：D-008（2026-09-21 用户确认）：全书人物"老周"更名"达哥"。
- 目标与范围：workspace/yocto/ 中除 task02（chapter 1，并入 T-006 本轮统一修订）外的 18 个文件共 381 处"老周"更名为"达哥"（登记时 grep 计数，执行时以实际为准）；只改称呼，不改人物关系、语气与叙事语义；台账与历史成果中的"老周"不改。
- 输入与依赖：D-008；书稿当前版本。与 T-006 评审轮不冲突（不动 chapter 1 受评正文）；与其他章节的用户通读校验并行时，按文件逐个提交以便追溯。
- 优先级：未设定
- 完成条件与确认方式：18 个文件全部处理，全仓 grep "老周" 在书稿目录零残留（证据写入任务）；改动语义中性由 writer 自查，project-manager 复核 grep 证据与抽查上下文；如需逐章过审，随各章 P3 评审轮覆盖。
- 进展：2026-09-21 任务登记。2026-09-25 writer 会话执行完毕：替换前按出现次数统计共 381 处，与登记一致；上下文抽查确认全部为人物称呼（对话署名与叙事指代），无语义风险；逐文件 sed 替换并逐文件提交（8679147 之后 18 个提交，ae9052c 起至 bc6fa78，每提交信息含该文件实际处数）；首轮曾把行数误作处数写入 3 个提交信息，已 reset 重做修正。task02（chapter 1）未被触及，与 T-006 受评正文隔离成立。
- 成果与验证证据：18 个文件更名完成，合计 381 处（逐文件：14/36/24/26/27/34/28/19/34/18/22/10/13/15/17/26/17/1）；`grep -ro "老周" workspace/yocto` 残留为 0；Git 提交 ae9052c…bc6fa78 共 18 个可逐文件追溯。语义中性自查通过（纯称呼替换）。
- 阻塞与下一位行动人：无。2026-09-25 project-manager 复核通过：①grep 按出现次数复核——书稿目录 `老周` 零残留；`达哥` 合计 405 处 = T-007 十八文件 381 处（逐文件与提交信息处数全部一致）+ task02 24 处（T-006 改名结果），与原登记总数 405 吻合；②上下文抽查（task10 开场对白、task15 L336、task16 L751、task17 L546 等）称呼自然、语义中性，一行两处均为"……达哥。达哥……"式自然对话；③task02 未被 T-007 提交触及（提交区间 ae9052c…bc6fa78 不含 task02），与 T-006 隔离成立。逐章过审随各章 P3 评审轮覆盖。
- 更新日期：2026-09-25

## T-008：真实环境运行核查 chapter 1 环境信息（Docker 容器实测）
- 负责人：reviewer
- 状态：completed
- 授权来源与日期：2026-09-23 用户在 project-manager 会话指示："我想找个成员在真实的环境上运行一下，看看书稿中的环境信息是否能和真实环境对的上"；项目经理按 reviewer 岗位 scope（核查命令、配置与实验步骤准确性）安排。同日用户确认 Docker 环境可用并解除阻塞。
- 目标与范围：以入库的 projects/book-yocto/workspace/docker/dockerfile 构建镜像并启动容器，在容器内按 chapter 1（workspace/yocto/task02-1-把Yocto跑起来.md，统一修订后的最新版本）逐步实际运行命令，核对书稿环境信息与真实环境是否一致：系统依赖包清单与安装结果（1.1.1）、用户名/家目录/主机名提示符（oops、/home/oops、tiger）、`source oe-init-build-env` 行为、目录结构与版本号输出等；如实记录每条一致/不符及证据。只核查与记录，不修改书稿正文。
- 输入与依赖：workspace/docker/dockerfile；chapter 1 最新版本（建议在 T-006 统一修订完成后执行，使核对对象为回填后的真实值；若提前执行则按当前版本核对并标注）；本机可用的 Docker 环境。
- 优先级：未设定
- 完成条件与确认方式：产出逐条核查记录（一致/不符 + 命令与输出证据），不符项按评审问题格式登记位置、依据与影响；记录经 project-manager 复核后，不符项转 writer 处理（随 T-006 修订或另开任务）。
- 进展：2026-09-23 任务登记。环境核查：本机 Git Bash 与 Windows PATH 均无 docker 命令（CLI 实际位于 C:\Users\YinZh\AppData\Local\Programs\DockerDesktop\resources\bin\docker.exe）。2026-09-23 用户确认环境可用，实测 Docker Client/Server 29.8.0 正常；镜像 embedded:dev 已由该 dockerfile 构建，容器 books 存在（Exited 2 天，`docker start books` 后按用户给出的 `docker exec -it books /bin/bash` 进入）。阻塞解除，转 todo。2026-09-23 reviewer 会话执行完毕：`docker start books` 恢复容器后以 `docker exec` 逐条执行 chapter 1 统一修订版（Git 9772f9c）环境相关命令——24 个依赖包逐条 `sudo apt install -y` 全部 rc=0；whoami/HOME/hostname=oops、/home/oops、tiger 与书稿一致；locale 全项 en_US.UTF-8；`git clone -b scarthgap https://...` 成功（1m48s）、`git branch` 为 `* scarthgap`；`ls -1` 顶层 21 项与书稿逐字一致（兼作 T-006 R-1 实测证据）；`source oe-init-build-env` 后 PWD/BUILDDIR/which bitbake 三项与书稿一致；bblayers.conf 逐字一致；默认 `MACHINE ??= "qemux86-64"` 与"保留 debug-tweaks 默认"确认；BitBake 2.8.1。发现 3 个不符项（E-1/E-2/E-3）与 2 个观察项（O-1/O-2），逐条登记于核查记录。未修改书稿正文。
- 成果与验证证据：[T-008 chapter 1 环境核查记录](workspace/t008-chapter1-env-check.md)——14 项一致（含实测输出证据）、3 项不符（E-1：1.2.3 输出块列出实际不存在的 meta-yocto-bsp/recipes-core；E-2：1.3.3 show-layers 输出层名实为 core/yocto/yoctobsp 且列宽不同；E-3：1.3.1 conf/ 清单漏 conf-notes.txt/conf-summary.txt 且无省略标记）、2 项观察、未执行项（首次构建与 runqemu）已注明理由。
- 阻塞与下一位行动人：无。2026-09-23 project-manager 复核核查记录通过：14 项一致均有实测证据、3 个不符项定位/实测/影响/建议齐全、观察项与未执行项如实标注，符合完成条件；不符项 E-1~E-3 已按项目经理安排转入 T-006，由 writer 在定稿确认前修订（改动小、事实明确，依据本记录实测证据），reviewer 对三处修复做有界复核。任务完成。
- 更新日期：2026-09-23

## T-009：真实环境首次构建与启动核查 chapter 1 主线（2026-09-25 起改在远程服务器 tiger 实测）
- 负责人：reviewer（2026-09-25 用户明确由 reviewer 执行，不再另等专人）
- 状态：completed（2026-09-25 project-manager 复核核查记录通过）
- 授权来源与日期：2026-09-23 用户在 reviewer 会话指示："我是希望你真正在docker中跑一次"；同日用户决定"先把任务记下来，后面找专人来执行吧"——执行暂缓，任务登记待专人执行。2026-09-25 用户在 project-manager 会话明确解除暂缓并指定 reviewer 执行："不是让审校在实际环境上跑一遍吗，把跑过的结果记下来，然后再完善书稿"。2026-09-25 用户在 reviewer 会话指示改在远程服务器执行："使用ssh tiger登录到远程的服务器，然后执行下T-009"（执行环境由本机 Docker 容器 books 变更为远程服务器 tiger，192.168.3.120）。
- 目标与范围：在容器 books（镜像 embedded:dev）内按 chapter 1（Git 89ccb72，798 行）书稿步骤实际执行：按 1.3.2 配置 local.conf → `bitbake core-image-minimal` 首次全量构建 → 核对 1.4.3 任务数汇总行与 deploy 产物清单 → `runqemu qemuarm64 nographic slirp` 启动并核对 1.5.1 guest 登录、uname/cpuinfo/free/df/ls /bin 输出 → 1.5.2 构建后 build/ 目录结构。逐条登记一致/不符与实测证据，补 T-008 记录的未执行项；只核查与记录，不修改书稿正文。
- 输入与依赖：容器 books（环境已就位，见进展）；chapter 1 定稿前修订版（Git 89ccb72）；T-008 核查记录（workspace/t008-chapter1-env-check.md）。
- 优先级：未设定
- 完成条件与确认方式：产出逐条核查记录（一致/不符 + 命令与输出证据），不符项按评审问题格式登记；记录经 project-manager 复核后不符项转 writer 处理。
- 进展：2026-09-23 任务登记。同日 reviewer 会话已完成执行前准备并试起跑：①按书稿 1.3.2 配置 local.conf（MACHINE 原行改 qemuarm64；DL_DIR/SSTATE_DIR/TMPDIR 取消注释按书稿取值；BB_NUMBER_THREADS="8"、PARALLEL_MAKE="-j 8" 末尾追加；EXTRA_IMAGE_FEATURES 保留默认 debug-tweaks），`bitbake-getvar` 逐项验证生效值与书稿一致；②15:40:32 启动 `bitbake core-image-minimal`（容器内后台执行，日志 ~/t009-build.log），实际总任务数 4073（书稿示例 4059，点版本漂移属预期）；起跑即出现 `WARNING: You are running bitbake under WSLv2`（Docker Desktop WSL2 后端提示，不影响构建，应记入核查记录）；15:45 用户决定暂缓执行、改由专人执行，构建经 SIGINT 干净停止于任务 164/4073。**容器现状（专人接手直接可用）**：poky scarthgap 已克隆于 ~/workspace/poky（HEAD cbd62bb2a9，yocto-5.0.20-106）；24 个依赖包已装齐；local.conf 已按书稿配置；已保留 downloads 149M / sstate-cache 4.3M / tmp 579M 断点进度。续跑命令：`docker start books` 后 `docker exec -d books bash -c 'cd ~/workspace/poky && source oe-init-build-env ~/workspace/build/ && bitbake core-image-minimal > ~/t009-build.log 2>&1'`，bitbake 自动从断点续跑；构建完成后按目标与范围逐项核对（任务汇总行、tmp/deploy/images/qemuarm64/ 产物、runqemu 启动与 guest 内命令、build/ 目录结构）。注意宿主机构建负载较高（8 线程，1-3 小时）。2026-09-25 reviewer 会话按用户当日指示改在远程服务器 tiger 执行（环境勘察：Ubuntu 24.04.4 裸机、8 核 / 30Gi、home 可用 647G；用户 9-19 已在该机完成过一次全量构建，deploy 产物时间戳 20260919012616，无控制台留档）。执行：①local.conf 按书稿 1.3.2 落盘（MACHINE `?=`→`=`、DL_DIR/SSTATE_DIR/TMPDIR 取消注释、追加 BB_NUMBER_THREADS="8"/PARALLEL_MAKE="-j 8"；原件备份 local.conf.t009-bak），bitbake-getvar 七项生效值与书稿逐项一致；②断点续跑 `bitbake core-image-minimal` 成功，任务汇总行实测 `Attempted 4073 tasks of which 4073 didn't need to be rerun and all succeeded`（sstate 全命中约 90 秒；首次从零形态未复测，证据边界已标注）；③`runqemu qemuarm64 nographic slirp` 启动成功，guest 内 root 空密码登录、uname/cpuinfo/free/df/ls /bin、poweroff 逐项实测；④build 目录结构三类清单核对。发现 2 个不符项（N-1 登录横幅版本号 5.0.18 vs 实测 5.0.20 且无免责标注；N-2 1.5.2 清单含构建结束后不存在的 bitbake.lock）与 2 个观察项（O-1 任务数 4059 vs 4073 漂移；O-2 首建耗时未复测），逐条登记于核查记录。未修改书稿正文。
- 成果与验证证据：[T-009 chapter 1 首次构建与启动核查记录](workspace/t009-chapter1-build-check.md)（含逐项命令与实测输出证据）；远程原始日志 ~/t009-build-rerun.log、~/t009-runqemu.log（服务器 tiger 上留存供复核）；容器试起跑日志 ~/t009-build.log（容器内，至任务 164/4073，已被远程实测取代）。
- 阻塞与下一位行动人：无。2026-09-25 project-manager 复核核查记录通过：①范围覆盖完整——local.conf 七项、构建与任务汇总、deploy 产物、runqemu 启动与 guest 六项、build 目录结构、T-008 未执行项全部补齐，无漏项；②证据质量合格——逐项实测值/日志原文、执行方式与远程留痕声明、证据边界（断点续跑非首次从零、O-2 耗时未复测）如实标注；③不符项 N-1/N-2 按评审问题格式登记且定性正确（bitbake.lock 仅 BitBake 运行期存在；横幅无注释与同页 uname 行有注释的内部矛盾成立）；④观察项 O-1 与 T-008 试起跑 4073 互证；⑤核对对象 ff9c794 与仓库实况一致，执行期间书稿正文零改动。下一位行动人=writer（定稿修订：不符项 N-1/N-2 + 观察项 O-1 加注 + U-17~U-20 + U-3 删节方案与 A-004 v0.3 确认项，用户确认口径后进入 writer 会话触发）→ reviewer 有界复核 → 用户确认定稿。
- 更新日期：2026-09-25

## T-010：task02 列表记号归一与顺手修订（D-009）
- 负责人：writer
- 状态：completed（2026-09-26 project-manager 复核通过）
- 授权来源与日期：D-009（2026-09-25 用户确认样式一）：全书列表统一 `-` 紧凑风格，task02 的 21 处 `*` 松散列表归一。
- 目标与范围：①chapter 1（workspace/yocto/task02-1-把Yocto跑起来.md，定稿版 92ede7e，816 行）的 `*` + 项间空行松散列表全部改回 `-` 紧凑风格（2026-09-25 排查为 21 处 `*` 项，分布于 4 个列表）；只动列表记号与项间空行，正文内容零改动、渲染结构不变。②顺手处理 T-006 reviewer 不阻断观察：L483/L532 两条 💡 中 bare 小写 `scarthgap` 统一为 Scarthgap（"点版本更新"指发行版，按 A-004 v0.3 §4 条款三）。
- 输入与依赖：D-009；A-004 v0.4 §7 列表记号条款；chapter 1 定稿版。
- 优先级：未设定
- 完成条件与确认方式：task02 全文件 `*` 列表记号零残留（grep 校验），diff 仅限列表记号/空行与两处 💡 单词；project-manager 复核 diff 与 grep 证据。改动为格式归一，不需要评审轮。
- 进展：2026-09-25 任务登记。2026-09-26 writer 会话执行完毕：①4 个 `*` 松散列表（1.2.2 术语 3 项、1.3.2 local.conf 变量 7 项、1.7 小结 6 项、1.8 延伸阅读 4 项，共 20 项）归一为 `-` 紧凑风格，项间空行全部移除；L135 `* scarthgap` 为 `git branch` 输出块内容（当前分支标记），属程序输出原样保留，不在归一范围——登记口径"21 处"实为 20 处列表项 + 1 处代码块输出，执行以实际为准。②顺手项：L483 💡 的 bare 小写 `scarthgap` 改为 Scarthgap；L532 💡 在定稿修订时已写为 Scarthgap，无需再改（登记时两条均为小写，定稿修订先行带过了 L532）。diff 统计 21 增 37 删，逐行校验仅限列表记号、项间空行与一处单词，正文零改动。
- 成果与验证证据：task02 归一版（workspace/yocto/task02-1-把Yocto跑起来.md，800 行）；grep `^\* ` 残留仅 L135 代码块输出一处（保留正确）；diff 过滤校验输出仅含记号/空行/一处单词改动（证据随提交可查）。
- 阻塞与下一位行动人：无。2026-09-26 project-manager 复核通过（提交 0a10c01）：①diff 逐对核对——4 个列表 20 项全部 `-` 紧凑化，每项文字与改前逐字一致（含 1.8 节 `<url>` 包裹保留），项间空行移除，正文零改动；②grep `^\* ` 全文件仅 L135 一处，系 `git branch` 输出块内的当前分支标记（程序输出），保留正确，登记口径 21 处 = 20 列表项 + 1 代码块输出的差异解释成立；③L483 💡 `scarthgap`→`Scarthgap` 已改，L532 复核确认定稿修订时已为 Scarthgap；④全章行数 816→800（空行移除所致），无新增问题。chapter 1 至此与 D-009/A-004 v0.4 完全一致。
- 更新日期：2026-09-26

## T-011：chapter 2《读懂这个项目》修订与第 1 轮评审（P3 首章）
- 负责人：writer（自查修订与统一修订）；reviewer（技术审校与复核）；用户（通读意见与定稿确认）
- 状态：in_review（统一修订稳定版 Git `bc33817` 的 project-manager 流程复核与 reviewer 技术/实测复核均已通过；用户在定稿确认前新增 U-9~U-12，等待 writer 修订图表、正文与等宽输出并补充 A-004 规则，随后做针对性复核并再次交用户确认）
- 授权来源与日期：2026-09-26 用户在 project-manager 会话确认启动 P3（"登记吧"）；依据 A-002 v0.3 P3 阶段定义（按依赖序逐章推进，chapter 2→16）与 T-006 定型节奏。
- 目标与范围：以 chapter 2《读懂这个项目》（workspace/yocto/task03-2-读懂这个项目.md，583 行）走 T-006 定型并由 D-010 补充的完整章节闭环：writer 自查与修订 → 任务内交叉评审（reviewer 技术审校；project-manager 项目管理意见；用户通读意见 D-007 经 project-manager 汇集）+ T-012 真实环境核验 → writer 统一修订 → reviewer 复核 → 用户确认定稿。修订与审校依据：A-004 v0.4 全部条款（含 v0.3 §4 三条款、v0.4 §7 列表记号）+ T-006 口径族——①正文/对话弯引号（R-3 口径，逐章统一）；②`/home/<your-username>` 占位回填固化环境真实值（U-11 口径，本章 8 处含 1 处 💡，回填 `/home/oops` 并移除替换提示）；③概念名大小写（sstate 等）；④scarthgap 分支名/发行版分工（本章 2 处 Scarthgap）；⑤列表记号 `-` 紧凑（D-009）；⑥延伸阅读链接实测核查（U-16 建议，curl 状态码+标题核对）；⑦读者正文不暴露内部 `task NN`，并移除章末 Git tag 流程（D-011）。
- 输入与依赖：书稿当前版本；A-004 v0.4（approved）；A-003；T-006 全套评审记录（口径族出处）；D-010/T-012；D-011。chapter 2 无既有 C-W/V 挂账且不依赖 P0.5，但这不构成跳过真实环境核验的依据；受评稿现有 4 处读者可见 `task NN` 和 1 组章末 `git tag chapter2` 流程，均已纳入本轮统一修订。
- 优先级：未设定
- 完成条件与确认方式：本轮评审意见全部处理并复核通过；T-012 逐项核验完成，实测不符项已由 writer 处理并经 reviewer 复核；D-011 的内部流程清理已完成；定稿确认前新增的 U-9~U-12 完成修订及相应针对性复核；按 D-003/D-010，章节定稿需用户确认。
- 进展：2026-09-26 任务登记（P3 首章）。同日 writer 会话按用户指示"执行 T-011"完成作者自查修订并提交第 1 轮受评稿（Git `0f2c44c`，583 行）：①标题层级由 H2/H3/H4 升为 1×H1 + 7×H2 + 2×H3，符合 A-004 v0.4；②正文/对话 86 对 ASCII 直引号改为弯引号，代码块与行内代码保持原样；③`<your-username>` 实际共 10 处（登记时写 8 处，经逐处核对为 9 处路径 + 1 处替换提示）全部回填 `/home/oops`，替换提示改为固化环境说明；④全章原已无 `*` 列表，继续保持 `-` 紧凑风格；2 处 Scarthgap 均指发行版/版本语境，保持官方大写；全章无外部 URL，故本章没有延伸阅读链接可执行状态码/标题核查；⑤按官方 Yocto Project 5.0 / BitBake 2.8 资料修正技术表述：`MACHINE_FEATURES` / `DISTRO_FEATURES` 分别生效且 `COMBINED_FEATURES` 取相关交集、同名 recipe 由 `BBFILE_PRIORITY` 主导而非依赖 `BBLAYERS` 顺序、`IMAGE_INSTALL` 只管 rootfs 软件包而非 TF-A/U-Boot/内核 provider；同时修正增量重构、独立交付合规、QEMU 启动链产物、`core-image` 类继承等表述。未代写 reviewer、project-manager 或用户意见。2026-09-26 本轮全部输入到齐后，writer 按用户指示执行统一修订并提交新稳定版 Git `bc33817`（558 行）：逐项处理 U-1~U-8、R-1~R-6，R 问题引用 T-012 N-1~N-5/O-1 实测证据；新增 4 个本地 SVG，将旧表格/字符图统一为 4 个真实图片与 3 个正式表格；落实 D-010/D-011。逐项回复见本轮评审末尾。project-manager 与 reviewer 随后分别复核通过。用户在定稿确认前查看实际渲染图，新增 U-9：Fig-2-1 硬件域把互联关系画成了不合理的父子层级；用户进一步确认该图直接改用 ASCII 图。随后用户新增 U-10：同一段中的 `**PL011**`/PL011 排版不一致，要求制定全书通用准则；project-manager 核查 A-004 v0.4 后确认硬件型号加粗规则尚为空白，已登记 T-013 补充。用户继而新增 U-11：四仓库开发形式和目标启动路径已经确定，不应写成由未来仓库实现决定是否采用 BL1；核查后续 chapter 6/7 草稿确认目标路径明确包含 BL1，须同步修正 chapter 2 正文、Fig-2-1、Fig-2-2 与 Table-2-2。用户随后新增 U-12：`bitbake-layers show-layers` 的终端表格列未对齐，要求同类 ASCII 输出统一等宽对齐；chapter 2 定向检查仅发现该处表格型输出，规则并入 T-013 的 A-004 v0.5 补充范围。
- 成果与验证证据：①第 1 轮受评稿：`workspace/yocto/task03-2-读懂这个项目.md`，Git `0f2c44c`（583 行，94 增/94 删）；②统一修订稳定版：同一正文，Git `bc33817`（558 行），并新增 `workspace/yocto/images/chapter2-hardware-startup-map.svg`、`chapter2-boot-flow.svg`、`chapter2-software-stack-boundary.svg`、`chapter2-project-overview.svg`。验证：4 个 SVG 均通过 XML 解析并以 1200px 原始画布渲染检查，文字/连线无截断；正文 4 个 Fig 标签与 4 个本地图片引用一一对应，3 个纯表格使用 Table 编号，66 个代码围栏标记成对，图片链接全部存在，`task NN`、章末 `git tag chapter2`、源码块多余 `\"` 均为 0，`git diff --check` 通过。技术变更直接采用 T-012 固化环境结果，不把本次静态校验冒充新的远程实测。
- 阻塞与下一位行动人：无执行阻塞。project-manager 与 reviewer 对 Git `bc33817` 的既有复核结论仅适用于该版本；用户新增 U-9~U-12 后，chapter 2 尚不能定稿。下一位行动人 writer：①按 U-9/U-11 用 `text` 代码块中的 ASCII 图替换 Fig-2-1 SVG，图中采用已经确定的 Boot ROM → BL1 → BL2 路径并移除失去引用的文件；②按 U-11 同步修订 Fig-2-2、Table-2-2 及两处正文，不再把是否采用 BL1 写成待决定项；③按 U-10 将所指段落两处 PL011 均改为正文体；④按 U-12 对齐 `bitbake-layers show-layers` 文本表格；⑤执行 T-013 补充 A-004 的全书排版规则并提交新的稳定版本与处理回复。随后由 project-manager 复核呈现、排版和意见闭环，reviewer 针对启动链技术关系做复核，再交用户确认定稿及 A-004 新版本。
- 更新日期：2026-09-26

### 本轮评审：chapter 2 作者修订稿（Git `0f2c44c`，第 1 轮）
- 安排来源与日期：T-011 登记（2026-09-26，用户确认启动 P3），评审流程与参与方按任务目标和 D-007 执行。
- 受评成果与稳定版本：`workspace/yocto/task03-2-读懂这个项目.md`，Git `0f2c44c`，583 行。用户保证本轮全部意见提交前正文版本不变；writer 待意见到齐后统一修订。
- 参与成员、范围与完成条件：reviewer——技术事实、命令/变量/输出、跨章一致性与教学可执行性，重点复核 writer 本轮修正的 feature 关系、layer/recipe 优先级、image/provider 边界；project-manager——任务范围、章节结构、P3 流程与跨章联动，重点核对 D-011 的内部流程清理范围；用户——按 D-007 通读并提交建议/疑问，经 project-manager 汇集。每方须记录实际范围、依据、未覆盖项与结论；本轮意见全部提交后 writer 才统一修订。
- 作者提交说明：writer，2026-09-26。作者自查结论为可提交评审；本结论不替代独立技术审校、项目管理意见或用户定稿确认。
- 本轮进度与下一位行动人：2026-09-26 用户完成整章通读，U-1~U-8 为本轮完整意见集；reviewer 完成技术审校（结论 revise，R-1~R-6）与 T-012 实测（N-1~N-5/O-1）；project-manager 完成 T-012 复核及项目管理意见。全部输入到齐后 writer 已完成统一修订并提交稳定版 Git `bc33817`；project-manager 对统一修订的流程、用户意见、图表交付及 D-010/D-011 复核通过，reviewer 对技术与实测处理及 4 个 SVG 的技术映射复核通过。下一位行动人用户确认 chapter 2 定稿。

#### 用户的通读意见（D-007）
- 日期、评审依据与未覆盖项：2026-09-26；受评版本 Git `0f2c44c`。用户已完成整章通读，U-1~U-8 为本轮完整意见集；用户通读范围无未覆盖项。上述意见是统一修订输入，不代表当前受评稿已经定稿。
- U-1｜位置：开篇 L3、L13、L15，2.1 节 L25，以及 L121 `Fig-2-1 tiger 硬件框图（Yocto 项目映射版）`｜内容：开篇写“达哥从内网 wiki 上拉了一张图”，后文又多次以“这张图”“架构图”承接，但书稿中没有实际图片；项目内也没有任何图片资产或 Markdown 图片引用，所谓 Fig-2-1 实际只有一个表格标题和后续映射表，读者看不到叙事所依赖的硬件架构图｜影响：开场的观察对象缺失，2.1 的硬件映射过程和 2.2 对启动链的回看都失去视觉依据；`Fig-2-1` 名称与实际内容形态不符｜建议：由 writer 在统一修订时补入一张可随 Markdown 交付的本地 tiger 硬件/启动链架构图，并在正文中使用明确图片引用；图至少覆盖 Cortex-A53、DDR、NAND、SPI NOR、UART、RTC、Watchdog、I2C EEPROM 及 BL1/BL2/BL31/BL33/Linux/UBI rootfs 的关系。现有映射表可保留为图后的查阅表，不以虚构的内网 wiki 资源代替交付内容。
- U-2｜范围：chapter 2 及后续各章的技术内容｜内容：用户要求 chapter 2 中的部分信息在真实环境中检验，后续每个章节都执行同类核验｜处理口径：已登记 D-010；chapter 2 新建 T-012，由 reviewer 对受评版本的命令、配置、路径、输出和环境相关技术结论逐项实测，结果成为本章统一修订与定稿输入。后续章节任务登记时必须包含同类核验范围和证据要求，不能因原稿没有 C-W/V 注记而省略。
- U-3｜位置：2.2 节 L160，达哥解释 `qemuarm64` 与 tiger 启动链差异的对话｜内容：用户认为“但 tiger 按本书设定必须走完整启动链”表达生硬｜分析：该短语突然跳出人物对话，以作者口吻说明创作设定，却没有从技术目标解释为什么 tiger 需要完整启动链；同段“没有单独构建 tiger 所需的……”也容易把 `qemuarm64` 与 tiger 的产物关系绕在一起｜建议：改为技术因果自然落在人物对话中，例如：“`qemuarm64` 的 `runqemu` 配置让 QEMU 直接加载内核，不需要单独构建板级固件和 bootloader。tiger 的目标是复现真实开发板的完整启动流程，需要依次经过 TF-A、U-Boot 和 Linux 内核。你现在要关心的是：Yocto 项目怎么为这些阶段选择对应的 provider。”最终技术措辞须结合 T-012 实测与 reviewer 意见确定。
- U-4｜位置：2.2 节 L185~L196，`Fig-2-2 启动链与 Yocto 项目构建产物对照`｜内容：用户指出此处也应让图题与实际图对应；当前只有 `Fig-2-2` 图题及一张启动阶段对照表，没有实际启动链图｜影响：正文称“画了第二张表”，图题却标 `Fig-2-2`，读者既看不到启动链的流程关系，也无法判断该编号指表格还是缺失图片｜建议：补一张与图题直接对应的启动链流程图，按 BL1/Boot ROM → BL2/TF-A → BL31/TF-A → BL33/U-Boot → Linux Kernel → UBI rootfs 展示加载次序，并在各节点标出 Yocto 项目产物与 `qemuarm64` 对照；现有表格保留为图后的详细对照表。统一修订时一并审计 Fig-2-1~Fig-2-5：Fig-2-1/2/4 当前实际是表格，Fig-2-3/5 是文本框图；所有 `Fig` 编号必须紧邻并明确对应真实图片或可渲染图，纯表格则使用表格编号，正文中的“图/表”称谓同步统一。
- U-5｜位置：2.3 节 L214，达哥解释应用改动不应进入 BSP 层的对话｜内容：用户认为“应用团队只是想改一个 API 响应”承接突兀｜分析：前句罗列重新构建 rootfs、验证和 license 清单的成本，后句却只陈述应用团队的意图，没有明确说出“小改动不应牵动整套固件流程”这一对比结论，因果链在落点前断开｜建议：把对比关系直接写进人物话语，例如：“重新 `bitbake` 会重跑受影响的任务、重新生成 rootfs 镜像，后面还跟着验证和 license 清单更新。应用团队改一个 API 的返回内容，不该牵动整套固件流程。”达哥顿了顿，“所以，`meta-tiger` 只放 BSP 层的东西——让板子能启动、能跑内核、有基础文件系统。应用程序是另一回事。”涉及构建与 license 清单的技术范围由 T-012/reviewer 核实。
- U-6｜位置：2.3 节 L231~L244，`Fig-2-3 软件栈三层归属图`｜内容：用户指出此处存在与前两幅图相同的问题｜现状与影响：图题下虽然有一个 `text` 代码块绘制的三层字符框图，但它不是正式图片资产，显示效果依赖等宽字体和渲染器；作为 `Fig-2-3` 交付时仍缺少明确的图片引用，且与计划补充的 Fig-2-1/2 呈现方式不一致｜建议：将字符框图转换为本地可交付的矢量图，并以 Markdown 图片引用紧邻 `Fig-2-3` 图题；图中保持 Application、OS/BSP、Firmware/Bootloader 三层及“进/不进 `meta-tiger`”边界，文字与紧随其后的章节解释逐项一致。原字符框图可在矢量图确认后移除，避免同一信息重复。
- U-7｜位置：2.4 节 L248“task 02 里……”及章末 L569~L573“后续任务清单”｜内容：用户指出正文不应出现 task 04/05/06 等编号｜现状与影响：受评稿共有 4 处读者可见的 `task NN`，它们是文件和写作工作流标识，却与 chapter 编号并排呈现，迫使读者理解一套无教学意义的内部双编号；“后续任务清单”也更像项目台账而非章节衔接｜建议：L248 改为按 chapter 1 或自然上下文回指；章末改成“接下来的路线”等读者导向表述，只保留 chapter 3/4/5 及相应内容。内部文件名、index 映射与项目台账继续保留 task 编号。该口径已上升为 D-011，后续各章自动检查。
- U-8｜位置：章末 L575~L580，`git tag chapter2` 叙事与命令块｜内容：用户确认 tag 不需要再打，并要求后续每章自动处理｜处理口径：移除本章为书稿章节设置 tag 的叙事、注释与命令块；后续各章按 D-011 自动移除同类章末 tag 流程，无需逐章重复确认。实际工程或上游版本识别所必需的 tag 仍按教学语义保留。
- 处理状态：待 writer 在本轮意见到齐后统一修订；U-1/U-4/U-6 的图片形式、文件路径、图表编号与正文引用，U-2 的实测不符项，U-3/U-5 的最终技术措辞，以及 U-7/U-8 的内部流程清理均须在处理回复中说明，随后由用户与相关评审者复核。

#### reviewer 的意见
- 日期、评审依据与未覆盖项：2026-09-26。受评版本 Git `0f2c44c`（583 行，当前正文相对该提交无差异）；依据 A-004 v0.4、D-010、T-012 在远程 `tiger` 的 Poky Scarthgap 实测、Poky/BitBake/Linux 6.6.151 源码，以及 BitBake/Yocto Project/TF-A 官方资料。已逐项覆盖本章全部 shell 命令、配置片段、输出块和 writer 本轮重点修正的 feature、layer/recipe 优先级、image/provider 边界。未覆盖项：①四个开发态仓库和 `meta-tiger` 尚未创建，因此 tiger 专属 provider、设备模型和最终产物名只能检查设计边界，不能冒充实测；②U-1/U-4/U-6 所需图片尚不存在，本意见只检查图中技术映射要求，不评价未交付图片的视觉质量；③本章没有重新构建镜像的命令，本次仅检查既有产物，不把 T-009 既有构建冒充本次从零构建。
- 结论：**revise**。大部分查询命令和 writer 修正后的 feature、layer 优先级、`IMAGE_INSTALL`/provider 机制成立；但 R-1/R-3/R-4/R-5 是可见技术错误，R-2 会造成 layer 职责误解，需与用户 U-1~U-8 及 T-012 结果一起统一修订。
- 问题：
  - R-1（中，技术名词）｜位置：2.1 节 L91-117、L128｜事实与依据：Linux 6.6.151 实物中 PL011/PL031/at24 分别有 `amba-pl011.c`、`rtc-pl031.c`、`at24.c`，SPI NOR 当前实现位于 `drivers/mtd/spi-nor/`；不存在正文所称的独立 `m25p80` 驱动文件，`m25p80` 只作为芯片名/兼容标识保留｜影响：读者会按错误驱动名查源码和设备树｜建议：改为 SPI NOR 子系统并注明具体兼容串取决于最终器件/模型；若 tiger 确用 M25P80，只把它写成器件或兼容标识｜原稿责任人：writer。
  - R-2（中，layer 职责）｜位置：2.4 节 L262、L343-348｜事实与依据：`qemuarm64.conf` 与 `core-image-minimal.bb` 都来自 OE-Core `meta`；`meta-poky` 提供 Poky DISTRO；`meta-yocto-bsp` 虽在默认 `bblayers.conf` 启用，却不提供 qemuarm64 MACHINE｜影响：“三者叠加才凑出 qemuarm64”会把默认启用误写成功能依赖｜建议：按实际提供者分别说明三层职责，明确 `meta-yocto-bsp` 是可参考的其他 BSP，不是当前 qemuarm64 构建的必需 MACHINE 来源｜原稿责任人：writer。
  - R-3（中，源码准确性）｜位置：2.5 节 L377、L416-418、L429-430｜事实与依据：远程 `cat` 的真实 recipe 使用普通双引号；Markdown 代码围栏内的 `\"` 会直接显示反斜杠｜影响：标为源码输出的文本不可逐字复制｜建议：删除双引号前的多余反斜杠，只保留 BitBake 续行本身的反斜杠｜原稿责任人：writer。
  - R-4（中，可执行反例）｜位置：2.6.2 节 L471-530，重点 L503-507｜事实与依据：按正文在 `qemuarm64.conf` 追加 `DISTRO_FEATURES += "systemd"` 后执行原命令，`bitbake -e core-image-minimal` 返回 rc=1，实际报 `Nothing RPROVIDES 'udev'` 及目标无可构建 provider；测试后配置已恢复且 Poky 工作树干净｜影响：“BitBake 不会报错”与固定基线的真实行为相反，读者照做会直接失败｜建议：按实测展示越界配置造成 provider 不一致并失败，或重新设计并实测一个无冲突反例；不得保留假定成功输出｜原稿责任人：writer。
  - R-5（中，启动链模型）｜位置：2.2 节 L141、L189-196 与小结 L540｜事实与依据：正文把 Boot ROM 与 TF-A BL1 合并成一项并称其不由 Yocto 构建；TF-A 官方构建说明则列出平台可能产生的 `bl1.bin`、`bl2.bin`、`bl31.bin`，通用 FIP 名为 `fip.bin`，`flash.bin` 属平台集成命名；小结“每个阶段都会物化为 deploy 文件”还与表中 Boot ROM 无产物、自身 qemuarm64 实测无 TF-A/U-Boot 相矛盾｜影响：混淆不可变 Boot ROM、可选 TF-A BL1 与平台打包物，并把条件性产物说成必然｜建议：先确定 tiger 是 Boot ROM 直入 BL2 还是使用 TF-A BL1，再分项画图；将 FIP/flash 等名称标为平台相关、待四仓库实测定名；收窄小结为构建系统负责的阶段按 MACHINE/provider 与打包规则部署相应产物｜原稿责任人：writer。
  - R-6（低，实测输出）｜位置：2.4 节 L315-320｜事实与依据：固定基线实测 `MACHINE_FEATURES="alsa bluetooth usbgadget screen vfat rtc qemu-usermode"`，正文示例少 `rtc qemu-usermode`；`DISTRO_FEATURES` 与正文一致，`COMBINED_FEATURES` 实测为两者交集中的 `alsa bluetooth usbgadget vfat`｜影响：已有“具体值因版本略有不同”提示，故不阻断机制理解，但与本项目固化环境仍有出入｜建议：回填当前实测值并可顺带展示 `COMBINED_FEATURES`，让本节的交集说明有直接证据｜原稿责任人：writer。
- 与用户意见的关系：U-3 建议中的 qemuarm64 直接加载内核、无独立 TF-A/U-Boot 产物已由 V-4 实测支持，但 tiger 完整链措辞须同时按 R-5 收窄；U-5 的“应用小改动不应牵动整套固件流程”作为项目分层原则成立；U-1/U-4/U-6 补图时必须同步落实 R-1/R-2/R-5，避免把当前错误映射固化进图片。
- 核验记录：[T-012 chapter 2 真实环境核验记录](workspace/t012-chapter2-env-check.md)。

#### project-manager 的意见与 T-012 复核
- 日期、评审依据与未覆盖项：2026-09-26。依据 T-011 受评版本 Git `0f2c44c`、reviewer 提交 `db6b272`、[T-012 核验记录](workspace/t012-chapter2-env-check.md)、A-004 v0.4 及 D-007/D-010/D-011，核对任务范围、版本稳定性、覆盖清单、证据边界、问题流转、跨章影响和下一步依赖。未重新承担 reviewer 的专业技术审校，也未把本次复核写成对全部命令的第二次独立实测。
- 结论：**pass（项目管理范围）**。受评正文相对 Git `0f2c44c` 无差异；用户、reviewer 与 project-manager 三方意见均已提交，T-012 记录满足任务约定的结构与覆盖要求。T-011 的总体结论仍为 **revise**，须完成统一修订、复核和用户确认后才能定稿。
- T-012 复核结果：①执行者、日期、正文版本、远程环境、Poky/BitBake 基线、前置状态和证据边界齐全；②V-1~V-11 覆盖本章可执行命令、配置、输出、provider/layer/feature 查询及 D-011 已判定不适用的章末 tag，失败和未执行项未伪装为通过；③N-1~N-5 均有位置、事实/实测、影响和建议，O-1 明确为非阻断观察；④关键解析失败原文已摘录进仓库内记录，远程完整日志位置为 `tiger:/tmp/t012-machine-policy.log`，执行后工作树干净状态已记录；⑤四个开发态仓库和 `meta-tiger` 缺失导致的 tiger 专属未覆盖范围已显式声明，没有越界声称完成 D-001 的后续实测。
- 问题去重与流转：reviewer R-1~R-5 分别对应 T-012 N-1~N-5，R-6 对应 O-1，是“审校问题”与“实测证据”的两种视图，不按 12 个独立问题重复处理。writer 统一修订时以 R 编号回复，并引用对应 N/O 证据；图片 U-1/U-4/U-6 同时吸收 R-1/R-2/R-5，D-011 的 U-7/U-8 一并处理。
- 证据留存观察：project-manager 复核时尝试读取远程完整日志，SSH 当前返回连接拒绝，未能对该外部文件做二次读取；仓库内核验记录已保存关键错误原文、命令、环境和状态，因此不阻断本次复核。后续逐章核验应优先把关键原始日志保存到本项目 `workspace/`，避免远程临时路径成为单点依赖。
- 项目管理意见：章节结构、P3 流程和跨章规则没有新增问题；D-010 的逐章实测门槛和 D-011 的内部流程清理已正确进入修订输入。下一位行动人 writer，待统一修订后按影响范围安排 reviewer 与 project-manager 复核。

#### writer 的统一修订处理回复
- 日期、修订版本与范围：2026-09-26；作者 writer；统一修订稳定版 Git `bc33817`，正文 `workspace/yocto/task03-2-读懂这个项目.md`（558 行）及 4 个本地 SVG。以下回复逐项对应 U-1~U-8、R-1~R-6；R-1~R-5 分别引用 T-012 N-1~N-5，R-6 引用 O-1，不把同一事实重复计算为两组独立问题。作者自查结论为已处理，实际解决状态待 reviewer/project-manager 按范围复核。
- U-1｜**已修订，待复核**：删除虚构的“内网 wiki”资源依赖，开篇改为打开本章随项目交付的 Fig-2-1；新增 `images/chapter2-hardware-startup-map.svg` 并以 Markdown 引用，覆盖 Cortex-A53、DDR、NAND、SPI NOR、UART、RTC、Watchdog、I2C EEPROM，以及拆分后的 Boot ROM/可选 BL1/BL2/BL31/BL33/Linux/UBI rootfs。原映射表保留并改编号为 Table-2-1；图内 SPI NOR 与启动链表述同时落实 R-1/N-1、R-5/N-5。
- U-2｜**已纳入本章，跨章规则已确认**：本章没有另造作者实测，直接以 T-012 的 V-1~V-11、N-1~N-5/O-1 为证据修订正文；失败案例和固化环境变量均按实测回填。后续章节继续由 D-010 约束逐章真实环境核验，本次不越权改动后续任务登记。
- U-3｜**已修订，待复核**：将“按本书设定必须走完整启动链”改成技术因果：`qemuarm64` 的 `runqemu` 路径直接加载内核，tiger 的目标是复现真实板卡的 TF-A/U-Boot/Linux 流程；同时吸收 R-5/N-5，明确 Boot ROM 之后是否采用 TF-A BL1 由平台方案和后续实测决定，并将关注点落到 provider 与部署规则。
- U-4｜**已修订，待复核**：新增 `images/chapter2-boot-flow.svg`，并以 tiger 完整目标路径、可选 BL1 旁路、候选产物、qemuarm64 实测路径两条泳道呈现；详细内容保留为 Table-2-2。全章图表完成重审：现为 Fig-2-1~Fig-2-4 四个真实本地 SVG，原三个纯表格改为 Table-2-1~Table-2-3，原 Fig-2-5 项目全景字符图转为 SVG 并顺延为 Fig-2-4。图内同步落实 R-5/N-5，未把候选 tiger 文件名冒充实测。
- U-5｜**已修订，待复核**：对话补全“小型 API 改动不应被迫重跑受影响任务、重建 rootfs、重做固件验证并更新相应 license 清单”的因果落点，再引出 BSP 与应用独立维护和交付。
- U-6｜**已修订，待复核**：将原 Fig-2-3 字符框替换为 `images/chapter2-software-stack-boundary.svg`，明确 Application、OS/BSP、Firmware/Bootloader 三层和“进/不进 `meta-tiger`”边界；原 ASCII 图已移除，并在图下注明 MACHINE 与 DISTRO 的职责边界。
- U-7｜**已修订，待复核**：2.4 节回指改为“第一章”，章末改为“接下来的阅读路线”和第 3/4/5 章；正文及 4 个 SVG 中读者可见 `task NN` 为 0，落实 D-011。
- U-8｜**已修订，待复核**：删除章末打 tag 的叙事、注释和 `git tag chapter2` 命令块；正文中该流程为 0，落实 D-011。T-012 V-11 已按决定标为不适用，本次没有执行或补造 tag。
- R-1（对应 N-1）｜**已修订，待 reviewer 复核**：搜索注释、正文和 Table-2-1 统一改为 PL011=`amba-pl011.c`、PL031=`rtc-pl031.c`、EEPROM=`at24.c`、SPI NOR=`drivers/mtd/spi-nor/` 子系统；明确 M25P80 只能作为候选芯片或 `compatible` 标识，不再称独立驱动。Fig-2-1 只写“SPI NOR / 具体芯片待定”。
- R-2（对应 N-2）｜**已修订，待 reviewer 复核**：2.4 节和 Table-2-3 明确 `meta` 提供 `qemuarm64` MACHINE 与 `core-image-minimal`，`meta-poky` 提供 Poky DISTRO，`meta-yocto-bsp` 提供其他参考板 BSP；删除“三者叠加才凑出 qemuarm64”的依赖误述，并在 Fig-2-4 中标明 qemuarm64 MACHINE 来自 `meta`。
- R-3（对应 N-3）｜**已修订，待 reviewer 复核**：2.5 节 recipe 输出块删除双引号前多余的反斜杠，只保留 BitBake 行续接反斜杠；全章 `\"` 残留扫描为 0。
- R-4（对应 N-4）｜**已修订，待 reviewer 复核**：2.6.2 改为 T-012 的实际失败路径：先备份并追加 `DISTRO_FEATURES += "systemd"`，`bitbake -e core-image-minimal` 返回码 1，展示 `Nothing RPROVIDES 'udev'` 和目标无可构建 provider 两条关键错误；解释 init/device-manager 策略不一致，随后用备份恢复并以空的 `git status --short` 收尾。删除假定成功输出和“BitBake 不会报错”。
- R-5（对应 N-5）｜**已修订，待 reviewer 复核**：正文、Fig-2-1、Fig-2-2、Table-2-2 与小结均将 Boot ROM 和可选 TF-A BL1 分开；tiger 采用哪条路径明确标为待四仓库实现与实测定稿；`bl1.bin`/`bl2.bin`/`bl31.bin`/U-Boot/DTB/UBI 均改为按平台规则“可能部署”，`fip.bin` 标为常见 TF-A 打包产物，`flash.bin` 标为平台约定；小结收窄为构建系统控制的阶段由 MACHINE/provider/recipe/平台打包规则决定是否及如何部署。
- R-6（对应 O-1）｜**已修订，待 reviewer 复核**：2.4 节命令新增 `COMBINED_FEATURES` 查询，输出回填固定基线实测值：`MACHINE_FEATURES="alsa bluetooth usbgadget screen vfat rtc qemu-usermode"`、`COMBINED_FEATURES="alsa bluetooth usbgadget vfat"`；`DISTRO_FEATURES` 保留实测完整值，并用交集结果直接解释机制。
- 作者验证与待复核项：4 个 SVG 均通过 XML 解析及 1200px 原始尺寸渲染检查；正文图片链接全部存在，图/表编号连续且形态一致，66 个代码围栏标记成对，`task NN`、章末 tag 流程、`\"` 均无残留，`git diff --check` 通过。待 reviewer 复核 R-1~R-6/N-1~N-5/O-1 与图片技术映射，待 project-manager 复核 U-1~U-8、D-010/D-011 和流程证据；复核到齐后仍需用户确认定稿。

#### project-manager 对统一修订的复核
- 日期、复核版本与范围：2026-09-26；复核稳定版 Git `bc33817` 及 writer 处理回复提交 `87f5ab5`。范围为 U-1~U-8 的交付处理、D-010/D-011 落实、图表编号与本地资产、评审证据和后续依赖；不替代 reviewer 对 R-1~R-6/N-1~N-5/O-1 和图片技术映射的专业复核。
- 结论：**pass（项目管理与用户意见处理范围）**，未发现新增问题。chapter 2 仍处于 `in_review`，须等 reviewer 技术/实测复核通过并由用户确认后才能定稿。
- U-1/U-4/U-6 与图表交付：正文已删除虚构的内网 wiki 依赖；Fig-2-1~Fig-2-4 均紧邻真实本地 SVG 引用，Table-2-1~Table-2-3 独立编号。4 个 SVG 均可解析并按 1200px 宽度实际渲染查看，文字、连线、边框和说明未见截断或重叠；图片链接全部存在。Fig-2-1 覆盖用户指定硬件和启动阶段，Fig-2-2 明确 tiger/qemuarm64 两条路径，Fig-2-3 呈现三层归属，Fig-2-4 呈现仓库到部署产物的项目全景。
- U-2/U-3/U-5：T-012 已执行并经 project-manager 复核，writer 明确以其证据修订而未冒充新实测；“按本书设定”和突兀的 API 句均已替换为完整技术因果，处理回复能追溯至对应用户意见。最终技术措辞随 reviewer 复核确认。
- U-7/U-8 与 D-011：读者正文和 4 个 SVG 中不再出现 `task NN`；章末已改成按第 3/4/5 章组织的阅读路线，`git tag chapter2` 叙事、注释和命令块均已移除。内部文件名继续保留 task 标识，符合 D-011 边界。
- D-010 与证据闭环：R-1~R-5 分别引用 T-012 N-1~N-5，R-6 引用 O-1；writer 逐项回复未把同一事实重复计数。正文和图片稳定在 Git `bc33817`，当前版本相对该提交无差异；XML、图片链接、66 个代码围栏、图表数量和 `git diff --check` 的静态检查均复核通过。
- 下一位行动人：reviewer 对 Git `bc33817` 复核 R-1~R-6/N-1~N-5/O-1 及 4 个 SVG 的技术映射；通过后转用户确认 chapter 2 定稿。

#### reviewer 对统一修订的复核
- 日期、复核版本与范围：2026-09-26；复核稳定版 Git `bc33817`，当前正文与 4 个 SVG 相对该提交无差异。依据 reviewer 第 1 轮 R-1~R-6、T-012 N-1~N-5/O-1、T-012 固化环境记录、相关官方资料及项目四仓库/集成层边界，逐项核对正文、表格和 SVG 中的技术修订。本次未重新执行远程实验；R-4/R-6 的数值与错误原文按已完成并经 project-manager 复核的 T-012 证据核对。受本地浏览器 `file://` 安全策略和图像查看器不支持 SVG 的限制，本次未独立复查视觉渲染质量；已检查 4 个 SVG 的 XML、画布、全部技术标签、连线/旁路路径及正文引用，视觉布局沿用 project-manager 已完成的渲染复核。
- 结论：**pass（技术与实测处理范围）**。R-1~R-6 对应的 N-1~N-5/O-1 均已正确处理，未发现阻断 chapter 2 定稿的新技术问题；T-011 下一位行动人为用户，按 D-003/D-010 确认 chapter 2 定稿。
- R-1/N-1：**通过**。正文搜索说明、解释段和 Table-2-1 均将 PL011、PL031、at24 与 SPI NOR 映射到实际源码/子系统；`m25p80` 仅作为候选芯片或 `compatible` 标识。Fig-2-1 使用“SPI NOR / 具体芯片待定”，没有固化未验证器件。
- R-2/N-2：**通过**。正文和 Table-2-3 分别说明 `meta` 提供 `qemuarm64` MACHINE 与 `core-image-minimal`、`meta-poky` 提供 DISTRO 策略、`meta-yocto-bsp` 提供其他参考 BSP；已明确 `qemuarm64` 不依赖 `meta-yocto-bsp`。Fig-2-4 在 BitBake 区内分别标注 `qemuarm64` 的 MACHINE 来自 `meta`、`tiger-aarch64` 由 `meta-tiger` 提供，未把默认启用层误写成 MACHINE 来源。
- R-3/N-3：**通过**。两个 recipe 输出块已恢复普通双引号，只保留 BitBake 行续接反斜杠；全章字面量 `\"` 扫描为 0。
- R-4/N-4：**通过**。2.6.2 采用 T-012 的可恢复实验与实际 rc=1 结果，保留 `Nothing RPROVIDES 'udev'` 和目标无可构建 provider 两条关键错误；正文解释了仅追加 `systemd` 而未同步 init/device-manager 策略造成的 provider 不一致，并记录恢复后空的 `git status --short`，不再声称 BitBake 成功解析。
- R-5/N-5：**通过**。正文、Table-2-2、Fig-2-1 和 Fig-2-2 均拆分 SoC Boot ROM 与可选 TF-A BL1；Fig-2-2 还给出 Boot ROM 直达 BL2 的旁路。`bl1.bin`/`bl2.bin`/`bl31.bin`、U-Boot、DTB 和 UBI 文件均标为平台规则下的候选产物，`fip.bin` 与平台约定的 `flash.bin` 已分开，小结也收窄为按 MACHINE/provider/recipe/平台打包规则决定是否及如何部署。
- R-6/O-1：**通过**。查询命令已加入 `COMBINED_FEATURES`；正文逐字回填固化环境的 `MACHINE_FEATURES="alsa bluetooth usbgadget screen vfat rtc qemu-usermode"` 与 `COMBINED_FEATURES="alsa bluetooth usbgadget vfat"`，并按交集用途解释，没有写成简单拼接。
- 4 个 SVG 技术映射：**通过**。Fig-2-1 覆盖用户指定的八类硬件、设备树/MACHINE/provider 边界及带不确定性说明的启动链；Fig-2-2 将 tiger 目标链与 qemuarm64 实测路径分泳道，并明确可选 BL1 旁路和候选产物边界；Fig-2-3 的 Application/OS-BSP/Firmware 分层及 MACHINE/DISTRO 职责与正文一致；Fig-2-4 保留四个开发态源码仓库与 `meta-tiger` 集成态的区分，并按 MACHINE 分开 qemuarm64 参考产物与 tiger 完整链目标。共同 deploy 框表示各 MACHINE 实际适用的产物集合，结合分支框内的 `Image + ext4`/“完整启动链目标”标签，不构成 qemuarm64 必有固件或 bootloader 的承诺。
- 静态验证：4 个 SVG 引用一一对应且文件存在，4 个 XML 均可解析，画布/viewBox 均为 1200px 宽；66 个代码围栏标记成对；`task NN`、`git tag chapter2`、多余 `\"` 均为 0；`git diff --check` 通过。
- 非阻断跨章联动：后续 chapter 5 草稿 `workspace/yocto/task06-5-写第一份MACHINE配置.md` 仍有“BitBake 不拦你，系统策略被悄悄改了”的旧表述，与本次 R-4 的固定环境 rc=1 结果不一致。该文件不属于 Git `bc33817` 的 T-011 受评成果，不阻断 chapter 2；chapter 5 进入 P3 修订与逐章实测时须按本次证据同步更正。

### 用户定稿确认前追加意见（Git `bc33817`）
- 日期、来源与范围：2026-09-26；用户查看 Fig-2-1 的实际渲染图后提出硬件域层级问题，并进一步建议直接采用 ASCII 图；project-manager 评估后采纳。范围为 Fig-2-1 的呈现形式、硬件域分组、启动链在同一图中的表达及必要的相邻说明；正文其他内容和另外 3 个 SVG 不因本意见自动扩大修改范围。
- U-9｜位置：`Fig-2-1 tiger 硬件与启动链映射`｜内容：当前 SVG 以 `Cortex-A53 × 4` 为顶层节点向 DDR、NAND、SPI NOR 分叉，又从 NAND 所在一行下方引出 UART、RTC、Watchdog、I2C EEPROM，视觉上形成“存储和内存从属于 CPU、外设从属于 NAND”的父子层级；用户认为该层级关系不自然，并建议直接使用 ASCII 图。｜分析：图中本想表达板级部件的功能分组和互联，却使用了树形层级连线，导致连接关系被读成组成或归属关系，且与正文“CPU、内存、Flash、外设”的并列分类不一致。该图强调结构与顺序，ASCII 图可以更直接地表达并列分组和启动链，也便于随 tiger 平台实现细节修改及在 Git 中审查。｜确认的修订方案：保留 `Fig-2-1` 图题，用 `text` 代码块中的 ASCII 图替换当前 SVG。硬件部分以“tiger board hardware”为总入口，将“Compute（Cortex-A53 × 4）”“Memory / Storage（DDR、NAND、SPI NOR）”“Peripherals（UART、RTC、Watchdog、I2C EEPROM）”作为并列功能组，不画 CPU→内存/存储或 NAND→外设的父子连线；启动链按 U-11 明确的 Boot ROM → TF-A BL1 → TF-A BL2 → TF-A BL31 → U-Boot BL33 → Linux Kernel → UBI rootfs 展开，不保留 Boot ROM 直达 BL2 的待定分支。图内优先使用 `+`、`-`、`|`、`->` 和短标签，中文解释放在图下正文，降低不同 Markdown 渲染器的 CJK 等宽对齐风险。不得新增未经实现或实测确认的产物名和打包细节；当前 SVG 在确认无其他引用后删除。
- U-10｜位置：2.1 节解释 `SERIAL_CONSOLES` 的对话，Git `bc33817` L77｜内容：同一段第一次写 `**PL011**`，第二次写 PL011，用户指出加粗不一致并要求确定一般准则。｜核查：A-004 v0.4 已规定术语首释、代码性标识反引号及概念名大小写，但没有规定 PL011、PL031、Cortex-A53 等硬件型号的粗体用法；全稿定向搜索显示这类型号在普通叙述中多数使用正文体，粗体主要出现在开篇并列强调或概念首释，现状缺少可复用边界。｜确认的执行口径：①粗体只用于首次正式定义的完整概念短语（如确需定义，可写 `**PL011 串口控制器**`）以及成组、结构化强调，不因字符串是硬件型号就单独加粗；②PL011、PL031、Cortex-A53、M25P80 等型号在普通叙述中使用正文体，后续引用同样用正文体；③变量、配置符号、设备节点、驱动文件、路径、命令和 `compatible` 字面值属于代码性标识，继续按 A-004 用反引号并保持原样，例如 `CONFIG_SERIAL_AMBA_PL011`、`ttyAMA0`、`amba-pl011.c`、`arm,pl011`；④代码块和程序输出保持原文，不套用正文粗体。按此口径，本段两处 PL011 均改为正文体。
- U-11｜位置：2.2 节 Git `bc33817` L145、L164，及 Fig-2-1、Fig-2-2、Table-2-2｜内容：原文称 Boot ROM 直达 BL2 或先经过 BL1 “要等四个开发仓库的实现和实测定稿”，用户指出开发形式已经确定，该说法模糊；用户随后指出 L164 “Boot ROM 之后是否采用 BL1，由平台方案决定”存在同样问题。｜核查与影响：D-001 已确认创建 `qemu-tiger`、`tf-a-tiger`、`u-boot-tiger`、`linux-tiger` 四个开发态仓库并经 `meta-tiger` 集成；后续 chapter 6 草稿进一步明确一次 TF-A 构建产出 `bl1.bin`/`bl2.bin`/`bl31.bin`，真实 SoC 由 Boot ROM 加载 BL1，QEMU 用 `-bios bl1.bin` 等效进入 BL1，chapter 7 也以该产物继续集成。因此待完成的是实现、验证和产物细节回填，不是开发形式或是否采用 BL1 的再次选择。现有 Fig-2-1/2 的可选 BL1、Boot ROM 直达 BL2 旁路和 Table-2-2 的“平台可选”会与后续既定设计冲突；L164 的“怎么选择 provider”还容易被理解成每个阶段都通过虚包仲裁。｜确认的修订方案：目标启动路径统一为 Boot ROM → TF-A BL1 → TF-A BL2 → TF-A BL31 → U-Boot BL33 → Linux Kernel → UBI rootfs。L145 建议改为：“tiger 的目标启动路径已经确定为 Boot ROM → TF-A BL1 → TF-A BL2 → TF-A BL31 → U-Boot BL33 → Linux Kernel → UBI rootfs。后续四个开发仓库的实现和全链实测，用于验证这条路径并回填具体产物与打包方式。”L164 建议改为：“`qemuarm64` 的 `runqemu` 配置让 QEMU 直接加载内核，没有单独构建板级 TF-A 和 U-Boot。tiger 的目标启动链已经确定：Boot ROM 加载 TF-A BL1，随后依次进入 BL2、BL31、U-Boot BL33 和 Linux。你现在要关心的是：Yocto 项目怎样为对应阶段接入配方、选择需要仲裁的 provider，并按平台规则部署产物。”Table-2-2 删除“平台可选/采用时”，Fig-2-1 ASCII 图和 Fig-2-2 SVG 删除直达 BL2 旁路。仍须明确这是目标设计，不能在 P0.5 尚未完成时写成已经实测通过。
- U-12｜位置：2.4 节 `bitbake-layers show-layers` 的 `text` 输出块，Git `bc33817` L250~L256｜内容：用户指出 `layer`、`path`、`priority` 三列的 ASCII 输出没有对齐，并要求同类格式统一。｜核查：chapter 2 当前表头的 `path`/`priority` 分别位于零基索引 21/92，三条数据行的 priority 值却位于 83/82/82，列边界确实不一致。chapter 1 已保存同一固化环境、同一命令的 T-008 实测原文并经 T-006 E-2 逐字复核：表头 `path`/`priority` 位于零基索引 22/94，三条数据行的值均位于索引 94；表头、分隔线、数据行长度分别为 102/104/95 字符。chapter 2 其余 `text` 块为普通输出或错误摘要，没有第二处同类表格。｜确认的执行口径：本处直接逐字复用 chapter 1 已验证的五行输出，不重新设计列宽或分隔线。全书规则区分两类内容：①真实命令输出的内容、顺序、列间空格和分隔线按实际输出保留；同一环境、同一命令的输出重复出现时复制已验证块，不手工重排；②作者绘制的 ASCII 图表使用带 `text` 标记的等宽代码块，按固定列宽用空格对齐，不使用 Tab，也不依赖行尾空格。这样既满足列对齐，也不把排版整理冒充实测原文。
- U-13｜位置：2.5 节 `DISTRO_FEATURES`、`MACHINE_FEATURES` 与 `COMBINED_FEATURES` 实测输出后的解释段，Git `bc33817` L323｜内容：用户认为现有表述不够清晰。｜分析：原段以两份列表的长度差开场，却没有说明长度本身不构成判断依据；随后把变量职责、recipe/class 的检查方式、当前实测交集和“不是简单拼接”压在一起，读者不容易看出三者关系。｜确认的修订文本：“先别比较列表长短，先看两份变量各自回答什么。”达哥指着输出，“`MACHINE_FEATURES` 描述机器具备的硬件能力，`DISTRO_FEATURES` 描述发行版准备启用的软件功能。大多数 recipe 和 class 会按自己的需要检查其中一份；只有同时依赖硬件能力和发行版策略的特性，才通过 `COMBINED_FEATURES` 判断两边是否都已声明。这个环境里，两边共同出现的是 `alsa`、`bluetooth`、`usbgadget` 和 `vfat`，所以 `COMBINED_FEATURES` 只有这四项。它不是把前两份列表拼起来，而是给需要同时满足两边条件的构建逻辑使用。”实测输出值保持不变。
- U-14｜位置：紧接 U-13 的同名 recipe 与 layer 优先级解释段，Git `bc33817` L327｜内容：用户认为该段也存在同样的不清晰问题。｜分析：原段先讲 `BBFILE_PRIORITY`，随后连续引入 `BBLAYERS`、版本、provider、普通变量和 `.bbappend`，其中“需要覆盖时”的对象不明确，也容易让读者误以为这些配置共同决定同一种选择。｜确认的修订文本：“先分清‘启用了哪些层’和‘同名 recipe 选哪一个’。”达哥打开 `bblayers.conf`，“`BBLAYERS` 只列出参与构建的 layer，它们在文件中的先后顺序不用于决定同名 recipe 的选择。两个 layer 都提供同一个 recipe 时，BitBake 比较各 layer 的 `BBFILE_PRIORITY`，数值较大的优先；这个优先级高于 recipe 版本号。若需要在多个版本或多个 provider 之间选择，再分别使用 `PREFERRED_VERSION` 或 `PREFERRED_PROVIDER`。普通变量和 `.bbappend` 的合并另有规则，不能套用这里的结论。”
- U-15｜位置：2.6.2 节实验报错后的解释段，Git `bc33817` L504｜内容：用户看完现有说明后仍不清楚错误原因，要求把示例的因果链进一步说明。｜源码核查：本章固化环境使用 Poky Scarthgap `77d1feb37e`，默认 `INIT_MANAGER` 为 `sysvinit`；`packagegroup-core-boot` 的 `VIRTUAL-RUNTIME_dev_manager` 默认是 `udev`。`default-providers.inc` 在 `DISTRO_FEATURES` 含 `systemd` 时把 `PREFERRED_PROVIDER_udev` 改为 systemd，而 systemd recipe 通过 `features_check` 同时要求 `systemd` 和 `usrmerge`。实验只追加 `systemd`，没有像 `init-manager-systemd.inc` 那样同时加入 `usrmerge`、停止回填 `sysvinit` 并统一 init/device-manager 等 runtime provider，因此 systemd recipe 因缺少 `usrmerge` 被跳过，原 eudev 又不再是 `udev` 的首选 recipe；包组保留的 `udev` 运行时依赖最终没有可构建 provider。｜确认的修订方案：保留实测报错，在其后用四步因果链明确说明：①当前仍是 SysVinit 配置，包组仍依赖 `udev`；②追加 `systemd` 使 `PREFERRED_PROVIDER_udev` 改选 systemd；③systemd recipe 因缺少同时必需的 `usrmerge` 而不可构建；④旧 provider 不再被选、新 provider 又不可构建，故出现 `Nothing RPROVIDES 'udev'`。随后明确正确做法是在 Distro 配置设置 `INIT_MANAGER = "systemd"`，由 Poky 加载 `init-manager-systemd.inc` 完成整套一致配置，而不是手工追加单个 feature。建议替换为：

  “这条语句的语法没有问题，错在它只添加了一个 feature，却没有完成 init 系统的整套切换。”达哥把变量关系列了出来：

  - Poky 当前仍按 `INIT_MANAGER = "sysvinit"` 加载 SysVinit 配置，`packagegroup-core-boot` 仍通过 `VIRTUAL-RUNTIME_dev_manager` 依赖 `udev`。
  - `DISTRO_FEATURES` 出现 `systemd` 后，`PREFERRED_PROVIDER_udev` 会改选 systemd recipe。
  - systemd recipe 同时要求 `systemd` 和 `usrmerge`；实验只追加了 `systemd`，所以 systemd recipe 因缺少 `usrmerge` 被判定为不可构建。
  - 原来的 eudev 不再是首选 provider，新的 systemd recipe 又不能构建，最终没有 provider 能满足包组对 `udev` 的依赖，`core-image-minimal` 因而在 provider 解析阶段失败。

  “所以，错误不在 `+=` 语法，而在于只拨动了 `systemd` 这个标志，留下了一组互相矛盾的策略。”达哥说，“切换 init manager 应在 Distro 配置里设置 `INIT_MANAGER = "systemd"`。Poky 随后加载 `init-manager-systemd.inc`，同时加入 `systemd` 和 `usrmerge`，停止回填 `sysvinit`，并把相关 runtime provider 一起切换。”
- U-16｜位置：2.7 节结尾“接下来的阅读路线”及其提示框，Git `bc33817` L552~L558｜内容：用户请 project-manager 判断该部分是否需要保留。｜判断：不需要，整体删除。其前一句“地图有了。下一步就是动手搭 `meta-tiger` 这个 layer”已经完成本章到 chapter 3 的过渡；再列 chapter 3~5 的内容与目录和各章开头重复，后续修订时还会增加章节预告漂移的维护点。提示框也不够准确：本章 2.6.2 已执行修改配置并恢复的验证实验，不能笼统说本章只“看”和“理解”；“所有动手创建工作”又容易被理解为全书此前没有动手操作。｜确认的修订方案：删除“接下来的阅读路线：”标题、三项列表及整个提示框，不另加替代段落，以达哥的“地图有了。下一步就是动手搭 `meta-tiger` 这个 layer。”作为本章收束句。
- 用户再次通读状态：2026-09-26 用户声明已经再次读完第 2 章；截至该声明，本轮定稿前追加意见为 U-9~U-16，已全部登记并形成处理口径。该声明表示本轮阅读输入已经到齐，不等同于批准当前 Git `bc33817`，因为上述意见尚未写入正文。
- 处理与复核安排：**U-9~U-16 均待 writer 修订，用户本轮阅读输入已到齐**。writer 须提交新的 chapter 2 稳定版本和逐项回复；U-10/U-12 的全书规则由 T-013 写入 A-004 新版本，既有后续章节不做脱离 P3 的一次性批量替换，随各章修订逐章执行。project-manager 复核 Markdown 呈现、ASCII 对齐、排版规则、结尾删减、跨章一致性和意见闭环，reviewer 针对 Fig-2-1/2、Table-2-2、U-13 的 feature 关系、U-14 的选择规则及 U-15 的 systemd/udev 失败链做技术复核。Git `bc33817` 上的既有 pass 不自动覆盖修订后的图表和正文；相应复核通过后再交用户确认 chapter 2 定稿及 A-004 新版本。

## T-012：真实环境核验 chapter 2 命令、配置与输出
- 负责人：reviewer
- 状态：completed（2026-09-26 project-manager 复核通过；不符项已转入 T-011，验证门槛随 T-011 修订与复核闭环）
- 授权来源与日期：2026-09-26 用户在 project-manager 会话明确要求 chapter 2 的信息在真实环境检验，并要求后续各章执行同类操作；项目经理据此登记 D-010 与本任务。
- 目标与范围：以 T-011 第 1 轮稳定受评版本 Git `0f2c44c` 为对象，盘点并执行 chapter 2 中可在真实环境核验的命令、配置、路径、变量/provider 解析、layer/recipe 查询和输出示例；逐项核对正文预期与实际结果。机制性结论同时核对对应版本官方文档或源码，但不以资料核查代替可执行项目的实测。reviewer 只提交核验记录与问题，不直接修改受评正文。
- 输入与依赖：T-011 受评稿 Git `0f2c44c`；D-010；A-004 v0.4；T-008/T-009 已使用的 Scarthgap 环境与证据格式。优先复用远程服务器 `tiger` 上既有 Poky Scarthgap 工作区，执行时须重新记录实际主机、系统、Poky/BitBake 提交或版本及构建目录状态；环境不可用时如实标 blocked，不沿用旧结果冒充本次执行。
- 优先级：未设定
- 完成条件与确认方式：形成逐项核验记录，至少包含执行者、日期、正文版本、环境基线、前置条件、命令/操作、预期、实际输出摘要、通过/失败/阻塞/未执行状态及原始证据位置；覆盖本章全部可执行命令与输出块，未执行/不适用项逐项说明。记录经 project-manager 复核；不符项转入 T-011，由 writer 统一修订并由 reviewer 复核后才可关闭 chapter 2 验证门槛。
- 进展：2026-09-26 任务登记。同日 reviewer 执行完毕：在远程 `tiger`（Ubuntu 24.04，Poky scarthgap `77d1feb37e`，BitBake 2.8.1，`MACHINE=qemuarm64`，`DISTRO=poky`）逐项执行本章全部查询命令并核对配置、路径、变量/provider、layer/recipe、镜像与包组源码、deploy 输出；对纯机制结论补查 Poky/BitBake/Linux 6.6.151 源码和官方资料。共登记 N-1~N-5 五个不符项与 O-1 一个非阻断观察。2.6.2 的原文反例按可恢复方式实测：追加 `DISTRO_FEATURES += "systemd"` 后，`bitbake -e core-image-minimal` 实际 rc=1（`udev` 无可构建 provider），与正文“不会报错”相反；测试结束自动恢复，Poky 工作树复核干净。章末 `git tag chapter2` 因 D-011 已确认移除，明确列为不适用而未执行。未修改受评正文。2026-09-26 project-manager 复核通过：版本稳定、范围覆盖、记录字段、证据边界和问题流转符合完成条件；远程完整日志在复核时因 SSH 连接拒绝未能二次读取，关键原文已保存在仓库内记录，此项作为后续证据留存改进，不阻断本任务完成。
- 成果与验证证据：[T-012 chapter 2 命令、配置与输出真实环境核验记录](workspace/t012-chapter2-env-check.md)；记录内含执行者、日期、正文版本、环境基线、逐项命令/预期/实际/状态、问题位置与建议。远程原始解析错误留存于 `tiger:/tmp/t012-machine-policy.log`；关键输出已摘录进核验记录，Poky 最终 `git status --short` 为空。
- 阻塞与下一位行动人：任务执行与记录复核均无阻塞，T-012 完成。不符项尚未解决，已转入 T-011；下一位行动人 writer 统一修订，随后由 reviewer 复核 R-1~R-6/N-1~N-5/O-1，project-manager 复核流程与证据闭环。
- 更新日期：2026-09-26

## T-013：补充硬件型号与等宽文本排版规则（A-004 v0.5）
- 负责人：writer
- 状态：todo
- 授权来源与日期：2026-09-26 用户在 project-manager 会话指出 chapter 2 同段 `**PL011**`/PL011 加粗不一致，要求确定一般准则；project-manager 依据 A-004 v0.4 和全稿定向搜索登记规则边界与实施任务。
- 目标与范围：将 T-011 U-10/U-12 的确认口径写入 A-004 新版本：硬件型号在普通叙述中使用正文体；粗体仅用于首次正式定义的完整概念短语或成组结构化强调；代码性标识使用反引号；真实命令输出保持内容与空白原文，同一已验证输出重复出现时逐字复用；作者绘制的 ASCII 图表在 `text` 代码块内使用固定列宽和空格对齐。同步在 chapter 2 落实两处 PL011 正文体，并以 chapter 1 已验证原文修复 `bitbake-layers show-layers` 三列表格。规则适用于全书后续 P3 修订，不在本任务中脱离逐章流程批量改写所有草稿。
- 输入与依赖：A-004 v0.4（approved）；T-011 U-10/U-12；chapter 2 当前稳定版 Git `bc33817`；全稿 PL011/PL031/Cortex-A53 等型号的定向搜索结果；chapter 2 等宽文本块定向检查结果。
- 优先级：未设定
- 完成条件与确认方式：A-004 更新版本、日期、来源、批准状态和版本记录，新增可执行的硬件型号/粗体/反引号边界、真实命令输出空白保真规则、作者 ASCII 图表等宽对齐规则及正反例；chapter 2 所指段和三列表格按规则修订；project-manager 核对规则与 U-10/U-12 一致，chapter 2 五行 `show-layers` 输出与 chapter 1 已验证块逐字一致，未误改实测内容、代码和输出值；A-004 新版本与 chapter 2 一并交用户确认。
- 进展：2026-09-26 任务登记。现状核查确认 A-004 v0.4 未覆盖硬件型号的粗体边界；chapter 2 L77 同段 `**PL011**`/PL011 混用，全稿同类型号以正文体为主。随后按 U-12 扩围：A-004 v0.4 也未明确等宽文本表格的列对齐规则，chapter 2 `show-layers` 输出的表头与数据列不一致；chapter 1 的同一命令输出已有 T-008 实测和 T-006 E-2 逐字复核，可作为直接修复基线。两项规则内容已在 T-011 U-10/U-12 固化，等待 writer 写入成果并应用。
- 成果与验证证据：待补充。
- 阻塞与下一位行动人：无。下一位行动人 writer，与 T-011 U-9~U-16 同轮处理并提交。
- 更新日期：2026-09-26

## 记录样式（不是真实任务）

```text
## T-001：任务标题
- 负责人：<member-id>
- 状态：todo
- 授权来源与日期：<用户直接指派或项目经理安排的可核对依据>
- 目标与范围：<实际授权的结果和边界>
- 输入与依赖：<任务、决定、资料版本；无则写无>
- 优先级：未设定
- 完成条件与确认方式：<可检查的条件；是否需评审，实际确认人>
- 进展：尚未开始
- 成果与验证证据：暂无
- 阻塞与下一位行动人：无
- 更新日期：<实际日期>
```

普通进展更新对应任务。更换负责人、重新打开或取消任务时保留原因、日期和已有成果；历史编号不复用。

## 可选的任务内评审样式（不是真实评审）

需要轻量评审时，将以下小节放到相应任务下；不要求为每位评审者另建任务、交接或报告。使用条件和写入责任见根项目运行协议第 4.1 节。

```text
### 本轮评审：<受评成果 ID、版本、轮次>
- 受评正文：<路径>
- 安排来源与日期：<用户或有权项目经理的明确安排>
- 参与成员、各自范围与完成条件：<member-id 及对应要求>
- 本轮进度与下一位行动人：<依据实际意见记录，不代写结论>

#### <member-id> 的意见
- 日期、评审依据与未覆盖项：<实际信息>
- 结论：<pass / revise / blocked；未提交时写尚未评审>
- 问题：<本轮唯一编号、位置、依据与影响、建议、原稿责任人；无问题则写无>
- 作者处理回复：<作者填写，注明问题编号与修订版本>
- 复核：<实际复核人、问题编号、版本、结果及证据；未复核则写待复核>
```

评审者只更新自己的意见与复核，作者填写处理回复；本轮全部意见提交后才修改受评正文。后续轮次保留前轮记录，不覆盖旧结论。
