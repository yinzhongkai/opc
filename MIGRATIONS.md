# 配置迁移

## 旧配置升级到版本 3

当前结构以 [CONFIG_SCHEMA.md](CONFIG_SCHEMA.md) 为准：TEAM 保存成员 ID 索引与项目岗位知识，每个成员文件仅含 id、role、scope。超级管理员使用根 [框架入口](SUPER_ADMIN.md)，不是项目岗位。

空框架直接使用版本 3 模板。已有版本 1 或 2 的项目需按用户授权迁移，校验器不自动改写，不能仅修改版本号。

1. 暂停迁移期间的共享写入，用 Git 提交或可恢复副本保存旧配置、岗位定义和工作记录。先由用户确认新版在册成员名单，不把旧配置中的所有条目自动当作当前参与成员。
2. 对确认保留的成员，从版本 1 的内嵌记录或版本 2 的成员文件中提取 id、role、scope，写入 `members/<id>.yaml`。旧成员 `status` 和 TEAM 的 `memberStates` 不进入新配置；原始记录保留在备份或历史档案中，不据此自动转交或完成任务。
3. TEAM.members 改为新版成员 ID 列表，移除旧 managedBy；不能登记 super-admin 岗位或其项目岗位知识。旧项目管理员的定义和责任来源保留在可恢复档案中，未完成工作由用户确认安排，不把旧管理员改名冒充业务成员。历史文件移位后核对引用，归档内容不放进当前 members 目录。
4. 保留项目约束、确认人、任务与成果历史，核对记录维护人和工作责任的实际安排。旧 ID 的历史记录注明原配置来源，不把未纳入名单的条目描述为当前成员。
5. 写入 schemaVersion 3，按新模板补齐说明文件，检查成员对应关系、岗位与知识引用和本地链接，运行校验。岗位与知识定义自身的 status 保持其原有规则。
6. 原成员身份未变的会话重新加载新版配置；框架管理员使用专用入口会话。迁移未完成或中断时报告实际进度与缺口，不让旧协议会话继续写入半完成的新配置。

迁移不删除业务成果，也不自动改写其他克隆。恢复时以保留的副本和后续实际变化为依据，不覆盖用户的新工作。
